# Poirot 全局结构树 — 从 main 装配到 graph 运行

```
main()                                                          ← app/cli/main.py 进程入口
│
├─ ① ensure_config(_PROJECT_ROOT)                               ← setup_wizard.py
│   ├─ .env 存在 → True
│   ├─ _has_any_provider_key()（Docker env_file 模式）→ True
│   └─ _run_wizard()
│       ├─ Panel 欢迎 → 选 provider（循环）→ 输入 API key
│       ├─ _ask_yes_no × 2（skill / mcp）→ _ask_sandbox()
│       ├─ _build_env_content(读 .env.example + overrides)
│       └─ env_path.write_text(content) → 写 .env
│   └─ False → return 1
│
├─ ② load_dotenv(_PROJECT_ROOT / ".env", override=True)          ← 重载使向导结果生效
│
├─ ③ argparse 解析
│   ├─ 全局：--provider / --model
│   ├─ run：question / --expert（默认 True）/ --no-expert /
│   │        --thread-id / --run-id / --logs-root / --no-artifact
│   └─ cli
│
├─ ④ 按 command 分发
│   ├─ None（无子命令） → run_chat(legacy=False) → TUI
│   ├─ "cli"            → run_chat(legacy=True)  → 传统 CLI
│   ├─ "run"            → bootstrap_runtime(expert_mode, provider, model, cli_overrides)
│   │                       └─ runtime.run_question(...) → LeaderAgent.run()
│   └─ 其他 → return 1
│
└─【路径 A/B】run_chat(provider, model, legacy)
    │
    ├─ stdout.reconfigure(utf-8)
    │
    └─ ⑤ runtime = bootstrap_runtime(provider, model)           ←【装配中心】
        │
        │ ═══════════════════════════════════════════════════════════════
        │ ⑤ bootstrap_runtime 装配全过程（同步阶段，在 asyncio.run 之前）
        │ ═══════════════════════════════════════════════════════════════
        │
        ├─ ⑤.1 load_config(expert_mode, cli_overrides) → AppConfig   ← config/loader.py
        │   ├─ selected_expert = cli_overrides > 参数 > False
        │   ├─ raw = deepcopy(DEFAULT_CONFIG)                    ← config/defaults.py
        │   ├─ selected_expert → _deep_merge(raw, EXPERT_PROFILE)
        │   ├─ _apply_cli_overrides(raw, overrides)
        │   │     ├─ expert_mode 类型校验 → 写 raw["runtime"]
        │   │     └─ flat_targets 白名单（logs_root / output_root /
        │   │        researcher_model / reporter_model / save_artifact）
        │   ├─ _validate(raw)
        │   │     ├─ researcher_model / reporter_model 非空
        │   │     ├─ expert_mode 为 bool
        │   │     ├─ max_loop_steps >= 1
        │   │     └─ logs_root 非空
        │   └─ _build_config(raw) → AppConfig                   ← config/schema.py
        │       ├─ RuntimeConfig / ModelConfig / ToolConfig /
        │       │   MiddlewareConfig / ReportingConfig /
        │       │   ObservabilityConfig / ContextGovernanceConfig
        │       ├─ sandbox = _build_sandbox_config()             ← POIROT_SANDBOX_* env
        │       ├─ memory  = _build_memory_config()              ← POIROT_MEMORY_* env
        │       └─ skill   = SkillConfig（默认工厂）
        │
        ├─ ⑤.2 ModelRouter(providers=None)                      ← config/model_router.py
        │   └─ providers = discover_available_providers()        ← config/provider_config.py
        │         ├─ _all_configs() → _resolve_profile(profile) × 12  ← provider_profile.py
        │         │     └─ 读 DEEPSEEK_API_KEY / OPENAI_API_KEY / ... env
        │         └─ 过滤 enabled 且 key 非空（或 _is_no_key），按 priority 升序
        │
        ├─ ⑤.3 装配 CapabilityRegistry                          ← capabilities/registry.py
        │   ├─ models = {researcher: router.build_model("researcher"),
        │   │             reporter:  router.build_model("reporter"),
        │   │             ...}
        │   │     └─ build_model(role)
        │   │         ├─ route_chain_for(role, providers)
        │   │         │     ├─ MODEL_ROUTES[role] 筛 by_name
        │   │         │     └─ 保证 deepseek 在链尾
        │   │         ├─ build_chat_model(p) × N
        │   │         │     ├─ get_provider_profile(p.provider)
        │   │         │     ├─ require_api_key()（非免 key）
        │   │         │     └─ kind 分发 → ChatDeepSeek / ChatOpenAI / ...
        │   │         └─ FallbackChatModel(models, provider_names) ← config/fallback_model.py
        │   │               └─ _should_fallback（瞬时错误降级）
        │   │
        │   ├─ tools = {builtin + registry 额外注册 + sandbox 工具}
        │   │     ├─ get_available_tools(groups=["core"])        ← agent_tools/available.py
        │   │     │     ├─ builtin.get_builtin_tools()
        │   │     │     │     └─ [web_search, read_snapshot, ask_help, skill_search]
        │   │     │     └─ dedupe_by_name + _tool_group 过滤
        │   │     └─ make_sandbox_tools(sandbox_provider)        ← sandbox/integration/tools.py
        │   │           └─ [bash, read_file, write_file, list_dir,
        │   │               str_replace, present_files]
        │   │
        │   ├─ reporter        = MarkdownReporter()              ← reporting/markdown_reporter.py
        │   ├─ artifact_store  = LocalArtifactStore()            ← artifacts/local_store.py
        │   ├─ sandbox_provider= 由 SandboxConfig.use 决定
        │   │     ├─ 空 → None
        │   │     ├─ "...:LocalSandboxProvider" → LocalSandboxProvider
        │   │     │     ├─ LocalRuntime
        │   │     │     ├─ LocalPathTranslator
        │   │     │     └─ AuditGuard(LocalSecurityGuard)
        │   │     └─ "...:DockerSandboxProvider" → DockerSandboxProvider
        │   │           ├─ LocalContainerBackend（基础设施 CRUD）
        │   │           │     └─ DockerExecutor（Local / Wsl）
        │   │           ├─ readiness（就绪轮询）
        │   │           └─ cross_process_lock（跨进程锁）
        │   ├─ skill_store     = 由 SkillConfig 决定
        │   │     └─ build_skill_manager() → SkillManager
        │   │           ├─ SQLiteSkillStore(config.db_path)
        │   │           ├─ load_startup(llm)
        │   │           │     ├─ store.discover(user_dirs, IMPORTED)
        │   │           │     │     └─ parser.parse_skill_file(SKILL.md)
        │   │           │     ├─ store.discover(core_dir, BUILTIN)
        │   │           │     ├─ SkillSelector(store, llm, max, threshold, min_sel)
        │   │           │     ├─ SkillInjectionMiddleware
        │   │           │     └─ SkillMetricsMiddleware
        │   │           ├─ set_eval_layer(EvalLayer)            ← bootstrap 回注
        │   │           └─ set_evolution_manager(EvolutionManager) ← bootstrap 回注
        │   ├─ specialist_registry = setup_multiagent(...)      ← multiagent/bootstrap.py
        │   │     ├─ setup_multiagent(config, agent_factory)     ← 三层嵌套装配
        │   │     │   ├─ metrics = MultiAgentMetricsStore(db_path)
        │   │     │   ├─ l2_setup = setup_l2(config, metrics)    ← L2 嵌套
        │   │     │   │   ├─ VersionDAG / FailureFocuser / EvolutionMutator
        │   │     │   │   ├─ PromotionGate(evaluator, version_dag, bridge)
        │   │     │   │   ├─ TriggerManager / L2TriggerMiddleware
        │   │     │   │   ├─ BudgetGuard / OrchestrationMetricsL2
        │   │     │   │   ├─ L2EvolutionWorker
        │   │     │   │   └─ l3_setup = setup_l3(...)            ← L3 嵌套
        │   │     │   │       ├─ SpecialistEvalRegistry + 3 adapter
        │   │     │   │       │     ├─ ProgrammaticAdapter
        │   │     │   │       │     ├─ LLMJudgeAdapter
        │   │     │   │       │     └─ LongitudinalPairsAdapter
        │   │     │   │       ├─ OrchestrationBridge（L2 调 L3 唯一入口）
        │   │     │   │       ├─ SpecialistRuntimeTracker
        │   │     │   │       ├─ DecisionLogWriter / Reader
        │   │     │   │       └─ MultiagentProgrammaticFacade
        │   │     │   ├─ for name in specialists_use:
        │   │     │   │     └─ _load_specialist(name)            ← 按 name 分发
        │   │     │   │         ├─ "pi"     → PiInstaller + PiCredentialProvider
        │   │     │   │         │              + PiRuntime + PiSpecialist
        │   │     │   │         ├─ "codex"  → CodexCredentialProvider
        │   │     │   │         │              + CodexRuntime + CodexSpecialist
        │   │     │   │         ├─ "claude" → ClaudeCredentialProvider
        │   │     │   │         │              + ClaudeCodeRuntime + ClaudeCodeSpecialist
        │   │     │   │         └─ "subagent" → SubagentRuntime(agent_factory)
        │   │     │   │                          + SubagentSpecialist
        │   │     │   ├─ registry.register(specialist) × N
        │   │     │   ├─ make_specialist_tool(...) × N → specialist_tools
        │   │     │   └─ subagent 兜底（若不在 specialists_use）
        │   │     ├─ OrchestrationMiddleware(...)
        │   │     └─ MultiAgentSetup(...)
        │   ├─ subagent_provider = SubagentRuntime | None
        │   └─ memory_provider = get_memory_provider()          ← memory/bootstrap.py
        │         ├─ config = get_memory_config()               ← memory/config.py
        │         ├─ build_default_provider(...)                ← memory/strategies/default/strategy.py
        │         │     ├─ EbbinghausDecayPolicy()
        │         │     ├─ CompositeForgetPolicy(decay)
        │         │     ├─ MarkdownFileStore(storage_path)
        │         │     ├─ HybridRetriever(store, decay)
        │         │     └─ DefaultMemoryManager(store, decay, forget, journal)
        │         └─ _wrap_store(store, retriever)              ← 闭环：写同步索引
        │
        ├─ ⑤.4 RunManager(AppConfig)                            ← runtime/run_manager.py
        │
        ├─ ⑤.5 make_lead_agent(                                ← leader/factory.py
        │     expert_mode, capability_registry,
        │     context_governance, sandbox_provider, artifact_server,
        │     mcp_audit_middleware, skill_injection_middleware,
        │     skill_metrics_middleware, specialist_tools,
        │     orchestration_middleware, memory_provider,
        │     memory_config, memory_worker)
        │   │
        │   ├─ registry = capability_registry
        │   ├─ resolved_expert = configurable.expert_mode 优先
        │   ├─ model = registry.get_model("researcher")
        │   │     └─ FallbackChatModel（researcher 链）
        │   ├─ summarize_model = registry.get_model(params.summarize_model) | None
        │   ├─ groups = ["core","deferred"]（expert）| ["core"]（default）
        │   ├─ tools = get_available_tools(groups)
        │   │           + registry.tools（额外注册）
        │   │           + specialist_tools（delegate_to_*）
        │   │
        │   ├─ middlewares = _build_middlewares(resolved_expert, ...)  ← 【中间件装配】
        │   │   │
        │   │   │   按挂载顺序：
        │   │   ├─ ① build_governance_middlewares(context_governance, model, summarize_model)
        │   │   │     └─ context_engineering/builder.py
        │   │   │         ├─ 公共 3 个 + StrategyMiddleware
        │   │   │         └─ （budget / compact / 熔断）
        │   │   ├─ ② SystemContextMiddleware()
        │   │   ├─ ③ skill_injection_middleware（条件）
        │   │   ├─ ④ skill_metrics_middleware（条件）
        │   │   ├─ ⑤ SkillActivationMiddleware（条件，同 ③）
        │   │   ├─ ⑥ TitleMiddleware()
        │   │   ├─ ⑦ RunJournalMiddleware()
        │   │   ├─ ⑧ mcp_audit_middleware（条件）
        │   │   ├─ ⑨ SandboxMiddleware(provider, artifact_server, sandbox_root)（条件）
        │   │   ├─ ⑩ MemoryMiddleware(memory_provider, config)（条件）  ← L4 记忆召回
        │   │   ├─ ⑪ MemoryConsolidationMiddleware(worker, trigger_n)（条件） ← L5 沉淀
        │   │   ├─ ⑫ HelpRequestMiddleware()
        │   │   ├─ ⑬ DanglingToolCallMiddleware()
        │   │   ├─    （LoopDetectionMiddleware 已移除）
        │   │   ├─ ⑭ ToolCallMiddleware()
        │   │   ├─ ⑮ orchestration_middleware（条件）           ← MultiAgent 派活拦截
        │   │   ├─ ⑯ EvidenceMiddleware()
        │   │   ├─ ⑰ StallDetectionMiddleware()                ← 停滞检测
        │   │   ├─ ⑱ TodoMiddleware(enforce_completion=expert)
        │   │   ├─ ⑲ ReflectionMiddleware(strategy=SufficiencyStrategy|LightReflection, llm=model)
        │   │   └─ ⑳ ReportMiddleware(model, auto_synthesize=expert)  ← expert 自动合成报告
        │   │
        │   ├─ system_prompt = apply_prompt_template(           ← leader/prompts.py
        │   │     expert_mode=resolved_expert,
        │   │     specialist_registry=_safe_get_specialist_registry(registry),
        │   │     skills_enabled=skill_injection_middleware is not None)
        │   │   ├─ pm.load("leader","identity")
        │   │   ├─ pm.load("leader","constraints")
        │   │   ├─ pm.load("leader","decision_guidance")
        │   │   ├─ expert → pm.load("leader","mode_expert")
        │   │   ├─ _build_specialist_routing_section(specialist_registry)（条件）
        │   │   └─ skills_enabled → pm.load("leader","skill_first_principle")
        │   │
        │   └─ LeaderAgent(
        │         graph = create_agent(                          ← LangGraph 建图
        │             model           = FallbackChatModel（researcher 链）
        │             tools           = list[BaseTool]（builtin + registry + specialist）
        │             middleware      = list[Middleware]（20 项）
        │             system_prompt   = 拼装结果
        │             state_schema    = ThreadState              ← state/types.py
        │             checkpointer    = get_checkpointer()       ← runtime/checkpointer.py
        │                               └─ InMemorySaver 单例
        │         ),
        │         capability_registry = registry)
        │
        ├─ ⑤.6 ArtifactServer(host, port).start()（可选）        ← artifacts/server.py
        │   ├─ 端口重试绑定
        │   ├─ ThreadingHTTPServer
        │   └─ 后台 daemon 线程 serve_forever
        │
        ├─ ⑤.7 组装 AppRuntime                                  ← 持有全部
        │   ├─ config: AppConfig
        │   ├─ model_router: ModelRouter
        │   ├─ capability_registry: CapabilityRegistry
        │   ├─ run_manager: RunManager
        │   ├─ leader_agent: LeaderAgent（内含 graph）
        │   ├─ thread_id / thread_dir / thread_journal
        │   ├─ skill_manager / mcp_manager
        │   ├─ memory_provider / memory_worker
        │   └─ artifact_server
        │
        └─ return AppRuntime
        │
        │ ═══════════════════════════════════════════════════════════════
        │ 装配完成 → 进入运行
        │ ═══════════════════════════════════════════════════════════════
        │
        ▼
    【路径 A：TUI】
    PoirotTUI(runtime=runtime, provider, model).run()
        └─ textual 全屏应用（独立事件循环）
        │
    【路径 B：传统 CLI】asyncio.run(_run_chat_async(runtime, provider, model))
        │
        ├─ ⑥ 主循环初始化
        │   ├─ console = Console()
        │   ├─ cli_state = {pending_* / skill_override / mode / model /
        │   │                current_tokens / current_fraction / current_window}
        │   ├─ _skill_names_provider()（闭包，读最新 runtime）
        │   ├─ session = PromptSession(
        │   │     completer=SlashCommandCompleter(get_registry(), skill_provider),
        │   │     bottom_toolbar=lambda: build_bottom_toolbar(cli_state),
        │   │     style=Style([...]))
        │   ├─ renderer = StreamRenderer(console, cli_state)
        │   ├─ intent_tree = default_intent_tree(report_handler=_handle_report_intent)
        │   ├─ console.print(render_banner("POIROT"))
        │   ├─ MCP 工具徽标（get_available_tools → 统计）
        │   └─ _print_welcome()（读 prompts/system/cli/welcome.md）
        │
        └─ ⑦ while True 主循环
            │
            ├─ user_input = await session.prompt_async("> ")
            │     └─ EOF / Ctrl+C → return 0
            │
            ├─ 【分支 A】prompt 以 "/" 开头
            │   ├─ handle_command(prompt, console, renderer, cli_state, runtime)
            │   │     └─ commands.py::handle_command → _registry.get(name) → handler(ctx)
            │   │           ├─ /help /clear /expand /thinking /tools /thread /prompt
            │   │           ├─ /expert /default → state["pending_expert_mode"]
            │   │           ├─ /report → state["pending_report"]
            │   │           ├─ /model → state["pending_model_switch"]
            │   │           ├─ /mcp reload → state["pending_mcp_reload"]
            │   │           ├─ /skill ... → runtime.skill_manager.*
            │   │           └─ /exit /quit → return True
            │   ├─ 消费 pending_*：
            │   │     ├─ pending_expert_mode → runtime.switch_expert_mode（复用 thread）
            │   │     ├─ pending_report → _trigger_report → generate_report_from_thread
            │   │     ├─ pending_mcp_reload → runtime.reload_mcp_tools
            │   │     └─ pending_model_switch → runtime.switch_model
            │   └─ continue
            │
            ├─ 【分支 B】普通输入
            │   ├─ renderer.render_user_input(prompt)
            │   ├─ intent_tree.detect_and_dispatch(prompt, runtime)
            │   │     └─ True → continue（不进 graph）
            │   │
            │   └─ 【流式研究】—— 一次完整运行
            │       │
            │       ├─ ctx = runtime.run_manager.create_run(        ← runtime/run_manager.py
            │       │     thread_id=runtime.thread_id,
            │       │     user_id="default-user",
            │       │     model_name=runtime.researcher_model_name,
            │       │     thread_dir=runtime.thread_dir)
            │       │   ├─ _make_run_id()（东八区时间戳 + 随机后缀）
            │       │   ├─ output_dir = {thread_dir}/runs/{run_id}
            │       │   ├─ RunJournal(run_id, events_path)
            │       │   ├─ RunContext(run_id, thread_id, config, journal, ...)
            │       │   ├─ RunRecord(status=PENDING, ...)
            │       │   └─ _write_record → record.json
            │       │
            │       ├─ runtime.run_manager.mark_running(ctx.run_id)
            │       │   └─ replace(record, status=RUNNING)
            │       │       + journal.append("run.started", ...)
            │       │
            │       ├─ config = _build_stream_config(runtime, ctx)  ← main.py
            │       │   └─ configurable: expert_mode / run_id / thread_id /
            │       │        journal / output_dir / plan_enabled /
            │       │        timezone / model（_resolve_actual_model_name）
            │       │   └─ recursion_limit = max_loop_steps * graph_node_multiplier
            │       │
            │       ├─ config["configurable"]["skill_override"] = cli_state["skill_override"]
            │       │
            │       ├─ client = PoirotStreamClient(                  ← services/stream_service.py
            │       │     graph=runtime.leader_agent.graph,
            │       │     config=config)
            │       │
            │       ├─ renderer.state["round_t0"] = time.monotonic()
            │       ├─ renderer.state["model"] = _resolve_actual_model_name(...)
            │       │
            │       ├─ async for event in client.stream(prompt):      ← ★ 驱动 graph
            │       │   │
            │       │   │   client.stream(question)
            │       │   │   │
            │       │   │   ├─ state = {messages:[HumanMessage], user_input,
            │       │   │   │            research_question}
            │       │   │   ├─ seen_ids / streamed_ids / seen_tool_call_ids 去重集合
            │       │   │   ├─ _answer_buffers / _answer_safe（selector 检测）
            │       │   │   │
            │       │   │   └─ async for (mode, chunk) in graph.astream(
            │       │   │         state, config,
            │       │   │         stream_mode=["values","messages","custom"]):
            │       │   │       │
            │       │   │       ├─【custom】middleware/strategy 发的自定义事件
            │       │   │       │     └─ yield StreamEvent(type=chunk["type"])
            │       │   │       │
            │       │   │       ├─【messages】token 级 delta
            │       │   │       │     ├─ 过滤 internal_llm tag
            │       │   │       │     ├─ AIMessageChunk：
            │       │   │       │     │     ├─ reasoning_content → thinking
            │       │   │       │     │     ├─ content delta → selector 缓冲 → answer
            │       │   │       │     │     └─ tool_calls → 去重 → tool_start
            │       │   │       │     └─ ToolMessage → tool_end
            │       │   │       │
            │       │   │       └─【values】完整状态快照
            │       │   │             ├─ budget → budget_update（过滤全零）
            │       │   │             ├─ sandbox → sandbox_update
            │       │   │             ├─ 首帧预填 seen_ids
            │       │   │             └─ 未流式消息 → answer/tool_start/tool_end
            │       │   │
            │       │   │   ═══════════ graph.astream 内部 ═══════════
            │       │   │   LangGraph 跑图：
            │       │   │   │
            │       │   │   ├─ 进入节点序列（middleware 按挂载顺序介入）
            │       │   │   │   ├─ before_model 钩子链
            │       │   │   │   │     ├─ SystemContextMiddleware
            │       │   │   │   │     ├─ SkillInjectionMiddleware（读路径：选择+注入）
            │       │   │   │   │     │     └─ SkillSelector.select_for_task
            │       │   │   │   │     │         ├─ override 强制包含
            │       │   │   │   │     │         ├─ quality filter（anti-loop）
            │       │   │   │   │     │         ├─ 候选 ≤ max → 全返
            │       │   │   │   │     │         ├─ LLM 选（候选 > max）
            │       │   │   │   │     │         └─ fallback → effective_rate 排序
            │       │   │   │   │     ├─ SandboxMiddleware（ContextVar 注入 sandbox_id）
            │       │   │   │   │     ├─ MemoryMiddleware.abefore_model（读路径）
            │       │   │   │   │     │     ├─ set_turn_id(turn_id)
            │       │   │   │   │     │     ├─ retriever.retrieve(MemoryQuery)
            │       │   │   │   │     │     │     ├─ store.list_all()
            │       │   │   │   │     │     │     ├─ decay.compute_strength()
            │       │   │   │   │     │     │     ├─ trace.with_strength()
            │       │   │   │   │     │     │     ├─ store.update() → _wrap_store
            │       │   │   │   │     │     │     │     └─ retriever.on_trace_updated()
            │       │   │   │   │     │     │     └─ RetrievalResult.compute_score()
            │       │   │   │   │     │     └─ 注入 memory_recall HumanMessage
            │       │   │   │   │     └─ ...（其余 before_model 钩子）
            │       │   │   │   │
            │       │   │   │   ├─ model 调用
            │       │   │   │   │     └─ FallbackChatModel.invoke/ainvoke
            │       │   │   │   │         ├─ 从 _active 轮转链内 provider
            │       │   │   │   │         ├─ ChatDeepSeek / ChatOpenAI / ...
            │       │   │   │   │         │     └─ 厂商 SDK 发请求、流式收 token
            │       │   │   │   │         ├─ 成功 → 更新 _active，返回
            │       │   │   │   │         └─ 失败 → _should_fallback → 降级下一个
            │       │   │   │   │
            │       │   │   │   ├─ 工具调用（ReAct 循环）
            │       │   │   │   │     ├─ ToolCallMiddleware
            │       │   │   │   │     ├─ OrchestrationMiddleware（若 delegate_to_*）
            │       │   │   │   │     │     ├─ _before_handler：set_current_state + 打点
            │       │   │   │   │     │     ├─ delegate_to_<name>（tools.py 闭包）
            │       │   │   │   │     │     │     ├─ get_current_state()
            │       │   │   │   │     │     │     ├─ budget_guard.check_and_record()
            │       │   │   │   │     │     │     ├─ ctx_summarizer.summarize()
            │       │   │   │   │     │     │     ├─ specialist.invoke() → runtime.invoke()
            │       │   │   │   │     │     │     ├─ res_summarizer.summarize()
            │       │   │   │   │     │     │     └─ decision_log_writer.write_async()
            │       │   │   │   │     │     └─ _after_handler：打点 + 归集产物
            │       │   │   │   │     ├─ SkillMetricsMiddleware.awrap_tool_call
            │       │   │   │   │     │     └─ 读 _active_skills_ctx → 写 _applied_ctx
            │       │   │   │   │     └─ 普通工具（web_search / sandbox / ...）
            │       │   │   │   │
            │       │   │   │   └─ after_model / after_agent 钩子链
            │       │   │   │         ├─ MemoryMiddleware.aafter_model
            │       │   │   │         │     └─ set_turn_id(None)
            │       │   │   │         ├─ MemoryConsolidationMiddleware.aafter_model
            │       │   │   │         │     └─ 每 N 轮 → worker.submit(MemoryTask)
            │       │   │   │         │           └─（异步）worker._process
            │       │   │   │         │                 ├─ manager.encode()
            │       │   │   │         │                 │     └─ store.add() → _wrap_store
            │       │   │   │         │                 ├─ manager.consolidate()
            │       │   │   │         │                 └─ set_turn_id(None)
            │       │   │   │         ├─ SkillMetricsMiddleware.after_agent
            │       │   │   │         │     └─ store.record_outcome（4 计数器）
            │       │   │   │         ├─ SkillJudgmentAnalyzer.analyze_execution（异步）
            │       │   │   │         │     └─ store.save_judgment + record_outcome
            │       │   │   │         ├─ TaskQualityJudge.judge_task（异步）
            │       │   │   │         │     └─ store.save_task_score
            │       │   │   │         ├─ L2TriggerMiddleware.after_model
            │       │   │   │         │     ├─ TriggerManager.should_trigger（四源 + 冷却）
            │       │   │   │         │     └─ 命中 → enqueue EvolutionTask
            │       │   │   │         ├─ ReportMiddleware.after_agent（expert）
            │       │   │   │         │     └─ 写 final_report 字段
            │       │   │   │         └─ RunJournalMiddleware（记 run.* / 其他事件）
            │       │   │   │
            │       │   │   └─ 返回 final_state（含 messages / final_report / ...）
            │       │   │
            │       │   ├─ flush 残留 answer buffer
            │       │   └─ yield done
            │       │
            │       └─ renderer.render(event)  ← 逐事件渲染
            │             ├─ thinking → 累积 → 切出时 flush "+ Thought: Xms"
            │             ├─ answer → 累积 full_answer
            │             ├─ tool_start → 开 Live spinner
            │             ├─ tool_end → 停 spinner + ✓ 摘要
            │             ├─ budget_update → _update_budget → cli_state
            │             │     └─ status_bar.build_bottom_toolbar（toolbar 刷新）
            │             ├─ done → Markdown 渲染 + 耗时尾行 + 分隔线
            │             └─ error → 红字
            │
            ├─ runtime.run_manager.mark_success(ctx.run_id)
            │   └─ replace(record, status=SUCCESS, finished_at, total_tokens)
            │       + journal.append("run.finished", usage)
            │
            ├─ console.print(run_id / events_path)
            │
            └─ 异常分支
                ├─ KeyboardInterrupt / CancelledError
                │     └─ mark_failed(ctx.run_id, "interrupted")
                └─ Exception
                      ├─ renderer._stop_spinner()
                      └─ mark_failed(ctx.run_id, str(exc))
```

---

## 装配 → 运行：三句话总览

**装配阶段（bootstrap_runtime，同步）**
```
load_config → AppConfig
  + ModelRouter（provider 链）
  + CapabilityRegistry（模型/工具/reporter/artifact/sandbox/skill/
                        specialist/subagent/memory 九类能力）
  + RunManager
  + make_lead_agent → LeaderAgent(graph = create_agent(
      model       ← FallbackChatModel（researcher 链）
      tools       ← builtin + registry + specialist_tools
      middleware  ← 20 项（按固定顺序）
      system_prompt ← 条件拼装
      state_schema  ← ThreadState
      checkpointer  ← InMemorySaver 单例
    ))
  = AppRuntime
```

**运行阶段（主循环 → 一次研究）**
```
用户输入 → intent 检测（未命中）→ create_run → mark_running
  → PoirotStreamClient.stream(question)
      → graph.astream(stream_mode=["values","messages","custom"])
          → LangGraph 跑图：
              before_model 钩子链（skill 注入 / memory 召回 / sandbox 绑定）
              → model 调用（FallbackChatModel → 厂商 SDK）
              → 工具调用（ReAct 循环 + Orchestration 派活 + Skill 打点）
              → after_model / after_agent 钩子链（memory 沉淀 / skill 评估 / L2 触发 / 报告合成）
          → 逐事件 yield StreamEvent
      → renderer.render(event) 逐事件渲染
  → mark_success / mark_failed
```