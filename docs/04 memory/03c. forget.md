---

name: Composite 遗忘策略（TTL + strength，两规则）。

description:

【整体职责】
实现 ForgetPolicy Protocol，提供记忆的自动遗忘判定。
是 memory 模块唯一的遗忘实现，被 manager 在 Phase 2 / 遗忘触发时调用。

【两规则组合】
should_forget 检查两条规则，命中任一即应遗忘：
1. TTL 过期：now - last_access > ttl_hours × 3600
2. strength 阈值：compute_strength(trace, now) < strength_threshold

【职责边界】
- 本模块只判「是否该忘」，不做实际删除（那是 store 的事）。
- 不含矛盾解决（resolve_conflict 已删）。矛盾解决走 reconsolidate（单条内容更新）
  或 consolidate（多条合并 + 旧标记 forgotten），由 manager 调用。
- 规则 2 依赖 DecayPolicy 计算当前 strength（构造时注入，缺省用 EbbinghausDecayPolicy）。
- 参数从 get_memory_config().forget 取（runtime 可切）；缺省回退 _constants.FORGET_THRESHOLDS。

【INVARIANT】
- 两规则遗忘：should_forget 检查 TTL + strength；无 resolve_conflict。
- 依赖 DecayPolicy 计算当前 strength（规则 2）。
- 参数 runtime 可切：每次从 get_memory_config().forget 取最新，不缓存。
- 缺省回退 _constants.FORGET_THRESHOLDS。

---

## 0. 结构树

```
forget.py —— Composite 遗忘策略（TTL + strength，两规则）
│
│   整体职责：实现 ForgetPolicy Protocol，提供记忆的自动遗忘判定。
│             是 memory 模块唯一的遗忘实现，被 manager 在 Phase 2 / 遗忘触发时调用。
│
│   两规则组合：
│       1. TTL 过期       ：now - last_access > ttl_hours × 3600
│       2. strength 阈值  ：compute_strength(trace, now) < strength_threshold
│
│   核心约定：只判「是否该忘」，不做实际删除。
│             不含矛盾解决（走 reconsolidate / consolidate）。
│
│
├── 导入的模块（判定所需）
│   │
│   ├── config.py · get_memory_config()
│   │     每次取最新 forget 阈值（runtime 可切）。
│   │
│   ├── schema.py · MemoryTrace
│   │     读 trace.last_accessed / created_at。
│   │
│   ├── _constants.py · FORGET_THRESHOLDS
│   │     缺省阈值（strength_threshold / ttl_hours / conflict_window_hours）。
│   │
│   └── decay.py · EbbinghausDecayPolicy
│       规则 2 需要算当前 strength（构造时注入，缺省内部构造）。
│
│
└── CompositeForgetPolicy（ForgetPolicy 的默认实现）
    │   两规则组合，不含矛盾解决。
    │
    ├── 字段
    │   └── _decay_policy : EbbinghausDecayPolicy
    │         规则 2 的强度计算依赖（构造时注入或默认构造）。
    │
    ├── __init__(decay_policy=None) → None
    │   │   注入衰减策略；未传入时构造缺省 EbbinghausDecayPolicy。
    │   │
    │   └── 引用的模块
    │       └── decay.py · EbbinghausDecayPolicy()
    │
    ├── should_forget(trace, now) → bool
    │   │   判是否应遗忘（两规则任一命中即 True）。
    │   │
    │   ├── 步骤
    │   │   ├── 1. 取阈值（runtime config 优先）
    │   │   ├── 2. 规则 1：TTL 过期 → True
    │   │   ├── 3. 规则 2：strength 低于阈值 → True
    │   │   └── 4. 两规则都未命中 → False
    │   │
    │   └── 引用的模块
    │       ├── config.py · get_memory_config()（间接，通过 _get_thresholds）
    │       ├── schema.py · MemoryTrace（读字段）
    │       └── decay.py · compute_strength()（规则 2 算强度）
    │
    └── _get_thresholds() → dict
        │   取遗忘阈值（runtime config 优先，缺省回退 _constants）。
        │
        ├── 步骤
        │   ├── 1. 取 config = get_memory_config()
        │   ├── 2. config.forget 非空 → 逐项取（缺失时回退 _constants）
        │   └── 3. 否则直接返回 _constants.FORGET_THRESHOLDS
        │
        └── 引用的模块
            ├── config.py · get_memory_config()
            └── _constants.py · FORGET_THRESHOLDS
```

---

## 1. 判定流程

```
should_forget(trace, now)
    │
    ├── _get_thresholds()
    │       ├── config.forget  ← runtime 覆盖（逐项 fallback）
    │       └── _constants.FORGET_THRESHOLDS  ← 缺省回退
    │       │
    │       ▼
    │   strength_threshold / ttl_hours / conflict_window_hours
    │
    ├── 规则 1：TTL 过期
    │       ├── last_access = last_accessed > 0 ? last_accessed : created_at
    │       ├── ttl_seconds = ttl_hours × 3600
    │       └── (now - last_access) > ttl_seconds → return True
    │
    ├── 规则 2：strength 阈值
    │       ├── current_strength = decay_policy.compute_strength(trace, now)
    │       └── current_strength < strength_threshold → return True
    │
    └── 都未命中 → return False
```

---

## 2. 被谁引用

```
CompositeForgetPolicy
│
└── strategies/default/manager.py · DefaultMemoryManager
      构造时接收实例（缺省内部构造）；
      在 Phase 2 / 遗忘触发时调 should_forget 判是否该忘。
```

---

## 3. 总览

```
CompositeForgetPolicy  = ForgetPolicy 的默认实现，两规则组合，不含矛盾解决
__init__               = 注入 decay_policy（缺省构造 EbbinghausDecayPolicy）
should_forget          = TTL 过期 或 strength 低于阈值 → True
_get_thresholds        = 取阈值（runtime config 优先，缺省回退 _constants）
```