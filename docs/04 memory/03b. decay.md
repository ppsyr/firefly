---

name: Ebbinghaus 衰减策略（lazy decay）。

description:

【整体职责】
实现 DecayPolicy Protocol，提供记忆强度的纯计算。
是 memory 模块唯一的衰减实现，被 forget / manager / retriever 共用。

【核心公式】
    strength = base_strength × (1 - decay_rate)^time_hours
             + log(1 + access_count) × access_boost
             + importance × importance_boost

    第一项：Ebbinghaus 衰减（随时间衰减）
    第二项：访问强化（被反复访问的记忆更强）
    第三项：重要性加成（语义上重要的记忆更强）

【lazy decay】
compute_strength 不修改 trace，只返回 float。
retrieve 时由 Retriever 调用，再用 MemoryTrace.with_strength() 更新。

【职责边界】
- 本模块只算 strength，不做遗忘判定（那是 ForgetPolicy 的事）。
- 不持有状态，无缓存，每次调用都是纯计算。
- 参数从 get_memory_config().decay 取（runtime 可切）；缺省回退 _constants.DECAY_PARAMS。
- 不修改 trace（frozen 语义，返回 float）。

【INVARIANT】
- 纯计算，不持有状态，线程安全。
- 参数 runtime 可切：每次从 get_memory_config().decay 取最新，不缓存。
- 缺省回退 _constants.DECAY_PARAMS。
- 返回值永远钳制在 [0.0, 1.0]。

---

## 0. 结构树

```
decay.py —— Ebbinghaus 衰减策略（lazy decay）
│
│   整体职责：实现 DecayPolicy Protocol，提供记忆强度的纯计算。
│             是 memory 模块唯一的衰减实现，被 forget / manager / retriever 共用。
│
│   核心公式：
│       strength = base_strength × (1 - decay_rate)^time_hours
│                + log(1 + access_count) × access_boost
│                + importance × importance_boost
│
│   核心约定：lazy decay —— compute_strength 不修改 trace，只返回 float。
│             retrieve 时由 Retriever 调用，再用 with_strength() 更新。
│
│
├── 导入的模块（计算所需）
│   │
│   ├── config.py · get_memory_config()
│   │     每次取最新 decay 参数（runtime 可切）。
│   │
│   ├── schema.py · MemoryTrace / MemoryType
│   │     读 trace.type / last_accessed / created_at / access_count / importance。
│   │
│   └── _constants.py · DECAY_COEFFICIENTS / DECAY_PARAMS
│       ├── DECAY_COEFFICIENTS  ← access_boost / importance_boost（公式系数）
│       └── DECAY_PARAMS        ← 缺省衰减参数（按类型）
│
│
└── EbbinghausDecayPolicy（DecayPolicy 的默认实现）
    │   纯计算，不持有状态，线程安全。
    │
    ├── compute_strength(trace, now) → float
    │   │   算当前强度，钳制在 [0.0, 1.0]。
    │   │
    │   ├── 步骤
    │   │   ├── 1. 按 trace.type 取衰减参数（runtime config 优先）
    │   │   ├── 2. 算 time_hours（自上次访问；未访问用 created_at）
    │   │   ├── 3. 算 Ebbinghaus 衰减项 base_strength × (1 - decay_rate)^time_hours
    │   │   ├── 4. 算访问强化项 log(1 + access_count) × access_boost
    │   │   ├── 5. 算重要性加成 importance × importance_boost
    │   │   └── 6. 三项相加，钳制到 [0, 1]
    │   │
    │   └── 引用的模块
    │       ├── config.py · get_memory_config()（间接，通过 _get_decay_params）
    │       ├── schema.py · MemoryTrace（读字段）
    │       └── _constants.py · DECAY_COEFFICIENTS（系数）
    │
    └── _get_decay_params(type) → dict
        │   取衰减参数（runtime config 优先，缺省回退 _constants）。
        │
        ├── 步骤
        │   ├── 1. 取 config = get_memory_config()
        │   ├── 2. type 转字符串 key（MemoryType → value，其他 → str）
        │   ├── 3. config.decay 中有该 key → 返回 config 值（runtime 覆盖）
        │   └── 4. 否则回退 _constants.DECAY_PARAMS[type_key]（缺省值）
        │
        └── 引用的模块
            ├── config.py · get_memory_config()
            ├── schema.py · MemoryType（类型转换）
            └── _constants.py · DECAY_PARAMS（缺省回退）
```

---

## 1. 公式计算流程

```
compute_strength(trace, now)
    │
    ├── _get_decay_params(trace.type)
    │       ├── config.decay[type]  ← runtime 覆盖（若有）
    │       └── _constants.DECAY_PARAMS[type]  ← 缺省回退
    │       │
    │       ▼
    │   base_strength / decay_rate
    │
    ├── time_hours
    │       ├── last_accessed > 0 → (now - last_accessed) / 3600
    │       └── last_accessed ≤ 0 → (now - created_at) / 3600
    │
    ├── 第 1 项：decayed_strength = base_strength × (1 - decay_rate)^time_hours
    ├── 第 2 项：access_boost = log(1 + access_count) × 0.1
    ├── 第 3 项：importance_boost = importance × 0.05
    │
    └── strength = 三项之和，钳制 [0.0, 1.0]
```

---

## 2. 被谁引用

```
EbbinghausDecayPolicy
│
├── strategies/default/forget.py · CompositeForgetPolicy
│     规则 2 用 compute_strength 算当前强度，判是否低于阈值。
│
├── strategies/default/manager.py · DefaultMemoryManager
│     构造时注入；encode 时取 decay 参数算 base_strength。
│
├── strategies/default/retriever.py · HybridRetriever
│     retrieve 时调 compute_strength 算当前强度，
│     再用 with_strength 更新 trace + 写回 store。
│
└── strategies/default/strategy.py · build_default_provider
      装配时构造实例，注入 forget / manager / retriever。
```

---

## 3. 总览

```
EbbinghausDecayPolicy  = DecayPolicy 的默认实现，纯计算无状态
compute_strength       = 算当前强度（Ebbinghaus + 访问强化 + 重要性），钳制 [0,1]
_get_decay_params      = 取衰减参数（runtime config 优先，缺省回退 _constants）
```