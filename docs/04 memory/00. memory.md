---

name: Memory 模块 — 长期记忆系统。

description:

────────────────────────────────────────────────────────────────
分层架构（north-star：三层解耦）
────────────────────────────────────────────────────────────────

各层落地状态：

- Layer 1（已落地）：基础可用性层
  Schema + 9 Protocol + 状态接入 + Registry slot

- Layer 2（已落地）：记忆管理层
  默认策略实现：EbbinghausDecayPolicy / CompositeForgetPolicy / DefaultMemoryManager

- Layer 3（已落地）：存储检索层
  MarkdownFileStore + HybridRetriever

- Layer 4（本模块当前状态）：中间件接入与 bootstrap 装配
  MemoryMiddleware + bootstrap lifecycle
  含集成层（MemoryMiddleware + bootstrap + 5 处 make_lead_agent 透传）

- Layer 5（规划中）：演化层
  Phase 2 LLM 决策 cron

- Layer 6（规划中）：扩展层
  adapter 具体实现 + Persona

依赖方向：
    app → agents/memory → (agents/capabilities, agents/state, agents/config)

约束：memory 包不反向依赖 `app`；跨层用 Protocol 破循环（boundary §3.1）。

────────────────────────────────────────────────────────────────
不变量（L1 + L2 + L3 + L4 合并）
────────────────────────────────────────────────────────────────


## L1 不变量（16 条）

1. **MemoryTrace 不可变**：
   frozen dataclass，strength 等可变字段通过 `with_strength()` / `with_operation()`
   创建新实例替换（类似 skill version DAG 的 is_active 指针）。

2. **四操作无 LLM**：
   Encode/Associate/Consolidate/Reconsolidate 是纯存储操作，LLM 生成内容外部传入
   （Retrieve 移至 Retriever）。

3. **检索是一切基础**：
   Phase 1 对话与 Phase 2 记忆管理都从 retrieve 开始（00 D4），retrieve 统一走 Retriever。

4. **lazy decay**：
   strength 在 retrieve 时按需计算，不跑后台衰减任务（00 §5.5）。

5. **Protocol 纯契约**：
   9 Protocol（7 核心 + 2 adapter）零实现，可 mock 可替换（00 D8）。

6. **strategies/default/ 同构**：
   与 context_engineering/strategies/default/ 同构，策略可插拔。

7. **CapabilityRegistry 注册**：
   memory_provider 作为第 9 个 capability，禁全局单例（00 D11）。

8. **CORE_FIELDS 保护**：
   recalled_memories / memory_updates 加入 CORE_FIELDS，防 metadata 冲突。

9. **向后兼容**：
   memory_provider=None / use="" 时 Poirot 行为不变。

10. **import 防火墙**：
    `agents/memory/` 不反向依赖 `app`（00 D12）。

11. **runtime 可切**：
    enable_recall/extract/token_budget/decay/forget/phase2 通过 `set_memory_config()`
    整替，Provider/Middleware/Retriever 不缓存 config，每次从 `get_memory_config()`
    取最新（参照 deer-flow memory_config.py 模式）。

12. **STARTUP_ONLY 仅 4 字段**：
    use/storage_path/vector_store/graph_store 换需重启（重建 Provider/adapter +
    重建 derived index）；其余 runtime 可切。

13. **叠加非互斥**：
    Markdown truth（总在）+ optional VectorStore + optional GraphStore 三层叠加，
    可同时启用（00 §8.2 derived shadow index），vector_store 与 graph_store 非三选一。

14. **adapter 是 Retriever 子组件**：
    VectorStore/GraphStore 非独立 MemoryProvider，由 HybridRetriever 组合；
    adapter 加载失败 → no-op 跳过 + log warning，系统不崩。

15. **lifecycle duck-type**：
    MemoryProvider/adapter 构造即就绪，无强制 initialize()；shutdown 走
    `hasattr(provider, "shutdown")` 检查，MarkdownFileStore 不实现，
    SQLiteShadowStore/VectorStore 实现（参照 deer-flow SandboxProvider 模式）。

16. **operation_log traceability**：
    MemoryTrace.operation_log 记录操作历史
    （encode/associate/consolidate/reconsolidate/forget），上限 20 条 FIFO；
    retrieve 不记（高频，强化在 strength/access_count 已体现）；
    actor 字段预留 turn_id（Layer 4 注入）；
    支持 debug 回溯"谁何时对哪条 trace 做了什么"。


## L2 不变量（20 条，默认策略实现层，承接 49 §8）

1. **四操作无 LLM**：
   Encode/Associate/Consolidate/Reconsolidate 是纯存储操作，
   merged_content/new_content 外部传入（00 D3）。

2. **Ebbinghaus 公式完整**：
   strength = base_strength×(1-decay_rate)^time_hours
            + log(1+access_count)×0.1
            + importance×0.05（00 §5.5）。

3. **lazy decay**：
   compute_strength 不修改 trace，retrieve 时由 Retriever 调用 + with_strength 更新
   （00 §5.5）。

4. **strength 钳制**：
   compute_strength 返回值永远在 [0.0, 1.0]。

5. **两规则遗忘**（B3）：
   should_forget 检查 TTL + strength（规则 1+2）；
   resolve_conflict 已删，矛盾解决走 reconsolidate/consolidate（00 §7.4）。

6. **矛盾不删除**：
   resolve_conflict 返回 new，old 标记 forgotten 不删除（保留回滚）（00 §5.3）。

7. **frozen 语义**：
   manager 所有操作返回依赖注入。

8. **依赖注入**：
   DefaultMemoryManager 接收 store + decay_policy + forget_policy + journal
   （策略组合可替换）。

9. **runtime 可切**：
   策略类不缓存 config，每次从 get_memory_config() 取（48 INVARIANT 11）。

10. **关联膨胀防护**（D3）：
    associate 检查 max_associations_per_trace，超限 LRU 淘汰最弱。

11. **consolidate 数量校验**（E1）：
    trace_ids 数量在 [min=2, max=10] 范围，否则抛 ValueError。

12. **reconsolidate 保留强度**（B3）：
    content 替换 + strength 保留原值 + last_accessed=now
    （视为一次访问，不重置 base_strength）+ operation_log 记 content diff
    （旧前 200 字符不丢）。

13. **shutdown duck-type**：
    DefaultMemoryProvider.shutdown() 委托 store/retriever（hasattr 检查）。

14. **encode id = content hash**（F2）：
    SHA256(content+type.value) 前 16 位，同内容同 type 自动去重。

15. **encode 幂等**（点4）：
    同 id 已存在 → 返回旧 trace，不重复 add，emit `memory.encode.duplicate`。

16. **associate LRU 淘汰**（D3）：
    超 max_associations_per_trace 时淘汰 strength 最低的关联（非静默跳过），
    保持关联质量。

17. **forgotten 标记不删除**（C1）：
    consolidate 后旧 trace 标记 `metadata.forgotten=True`，store 不删除（保留回滚），
    Retriever 过滤留 L3。

18. **traceability A**（operation_log）：
    manager 每操作 append OperationLog
    （encode/associate/consolidate/reconsolidate/forget），上限 20 条 FIFO；
    retrieve 不记。

19. **traceability B**（journal）：
    manager 每操作 emit `memory.*` 事件（journal callback 注入，Layer 4 接 RunJournal）。

20. **traceability C**（actor 预留）：
    OperationLog.actor + journal payload.actor 从 ContextVar `_turn_id_var` 取
    （Layer 4 MemoryMiddleware 注入 turn_id），Layer 2 测试时为 None。


## L3 不变量（12 条，存储检索实现层，承接 50 §9）

1. **Markdown-as-Truth**：
   traces.md 是 truth source，内存索引是 derived（可重建）（00 §8.2）。

2. **单文件 + 分隔符**：
   所有 trace 在 traces.md，用 `<!-- trace: {id} -->` 分隔（方案 B）。

3. **frontmatter + content**：
   每条 trace = YAML frontmatter（所有字段除 content）+ content 正文。

4. **无事务**（2A）：
   add/update/remove 逐个操作，失败 log，接受最终一致（Phase 2 cron 修复）。

5. **文件锁**（6B）：
   `threading.Lock` 保护 update/remove（单进程）；跨进程锁留后续。

6. **list_by_filter 粗筛**（7A）：
   store 只按 max_age_hours/type/metadata 过滤，strength 精算由调用方。

7. **retrieve 强化写回**（1A）：
   HybridRetriever.retrieve 内部调 store.update，caller 不负责。

8. **forgotten 过滤在 Retriever**（3B）：
   store 不感知 forgotten，Retriever 过滤 metadata.forgotten。

9. **BM25 增量索引**（5B）：
   构造全量建 + on_trace_* 增量维护。

10. **无 vector/graph**：
    HybridRetriever 纯 BM25，不依赖 adapters（空壳保留 Layer 6）。

11. **storage_path 锚定**（01 D12）：
    相对路径锚定（L3 cwd fallback，Layer 4 bootstrap 传绝对路径）。

12. **解析容错**（2A）：
    frontmatter 损坏 log + 跳过，不崩（最终一致）。


## L4 不变量（11 条，中间件接入与 bootstrap 装配层，承接 53 §8）

1. **记忆是 middleware**：
   MemoryMiddleware 挂载，不进 leader agent 主体（00 D9）。

2. **不进 system prompt cache**：
   per-call HumanMessage(hide_from_ui=True)，recalled_memories 只存索引（00 D10）。

3. **挂载顺序**：
   Sandbox 后，HelpRequest/ToolCall 前（记忆引用 sandbox 结果，不进 tool pairing）。

4. **set_turn_id 注入/清除**：
   before_model 注入，after_model 清除（traceability C）。

5. **懒加载双检锁**：
   get_memory_provider 线程安全。

6. **shutdown duck-type**：
   hasattr(provider, "shutdown") 委托 store/retriever。

7. **store 包装 5B**：
   add/update/remove 后调 retriever.on_trace_*（装饰器，不改 store 类）。

8. **5 处透传**：
   make_lead_agent 5 调用点都透传 memory_provider（漏传该路径记忆丢失）。

9. **storage_path 锚定**：
   _resolve_relative_paths 锚定 _PROJECT_ROOT（01 D12）。

10. **import 防火墙**：
    bootstrap.py 不 import app（00 D12）；
    MemoryMiddleware 迁到 agents/middlewares/memory_recall_middleware.py（块 B）。

11. **向后兼容**：
    use="" 默认禁用，行为不变；memory_provider=None 不挂载。


---

# 0. Memory 全链路引用

```
Memory
│
├── 主链① bootstrap.py —— 装配 + 生命周期
│   │
│   ├── get_memory_provider() —— 拿 provider
│   │   │   对外唯一入口，首次装配，之后复用；禁用时返 None。
│   │   │
│   │   ├── config.py · get_memory_config()
│   │   │     读 use 判启用、storage_path 定落盘、decay/forget 定参数。
│   │   │     └── _memory_config（配置数据，叶子）
│   │   │
│   │   ├── strategies/default/strategy.py · build_default_provider(journal=)
│   │   │     真正的装配函数，把下面组件拼成 provider。
│   │   │     │
│   │   │     ├── decay.py · EbbinghausDecayPolicy()
│   │   │     │     提供衰减策略（算强度）
│   │   │     │     ├── config.py（读 decay 参数）
│   │   │     │     └── _constants.py（缺省衰减参数）
│   │   │     │
│   │   │     ├── forget.py · CompositeForgetPolicy(decay)
│   │   │     │     提供遗忘策略（判是否该忘）
│   │   │     │     ├── decay.py（规则 2 算 strength）
│   │   │     │     ├── config.py（读 forget 阈值）
│   │   │     │     └── _constants.py（缺省阈值）
│   │   │     │
│   │   │     ├── store.py · MarkdownFileStore(config.storage_path)
│   │   │     │     提供持久化后端（Markdown truth source）
│   │   │     │     ├── _resolve_storage_path()（解析路径）
│   │   │     │     ├── _load()（启动加载 traces.md）
│   │   │     │     │     └── _parse_trace()（frontmatter → trace）
│   │   │     │     │        └── schema.py · MemoryType/Association/OperationLog/MemoryTrace（构造对象）
│   │   │     │     ├── exceptions.py · MemoryConflictError/MemoryNotFoundError（冲突/找不到时抛）
│   │   │     │     ├── schema.py（解析、序列化 trace）
│   │   │     │     └── types.py · MemoryFilter（list_by_filter 的过滤条件）
│   │   │     │
│   │   │     ├── retriever.py · HybridRetriever(store, decay)
│   │   │     │     提供检索后端（BM25 + 强化写回）
│   │   │     │     ├── _build_index_from_store()（冷启动全量建索引）
│   │   │     │     │     ├── store.py · list_all()（取全部 trace）
│   │   │     │     │     └── _index_trace()（单条入索引）
│   │   │     │     ├── memory_store.py · MemoryStore（候选来源契约）
│   │   │     │     ├── retriever.py · Retriever（实现契约）
│   │   │     │     ├── schema.py · MemoryTrace（读内容、派生强化 trace）
│   │   │     │     ├── _constants.py · BM25_PARAMS（k1/b/epsilon）
│   │   │     │     ├── decay.py（算 strength）
│   │   │     │     └── types.py · MemoryQuery/RetrievalResult（检索入参出参）
│   │   │     │
│   │   │     └── manager.py · DefaultMemoryManager(store, decay, forget, journal)
│   │   │           提供四操作编排（encode/associate/consolidate/reconsolidate）
│   │   │           ├── config.py（取 decay 参数）
│   │   │           ├── exceptions.py · MemoryNotFoundError（找不到 trace 时抛）
│   │   │           ├── memory_store.py · MemoryStore（依赖注入的持久化后端）
│   │   │           ├── schema.py · Association/MemoryTrace/MemoryType/OperationLog（构造派生、记日志）
│   │   │           ├── _constants.py · ASSOCIATE_DEFAULTS/CONSOLIDATE_PARAMS/DECAY_PARAMS
│   │   │           ├── decay.py / forget.py（策略实例，构造时注入）
│   │   │           ├── store.py · get/add/update/batch_update（四操作落到 store）
│   │   │           ├── bootstrap.py · _wrap_store（间接，写完同步索引）
│   │   │           └── journal（发 memory.* 事件）
│   │   │
│   │   └── _wrap_store(store, retriever)
│   │         装饰器模式，把 store 写操作同步到 retriever 增量索引。
│   │         读写闭环的关键：写完立刻能被检索到。
│   │         ├── store.py · add/update/batch_update/remove（包装原方法）
│   │         └── retriever.py · on_trace_added/on_trace_updated/on_trace_removed（通知索引同步）
│   │
│   ├── reset_memory_provider() —— 清缓存不关闭
│   │     测试用，缓存置 None，方便复用/替换。无项目内引用。
│   │
│   ├── shutdown_memory_provider() —— 关闭 + 清缓存
│   │     主链出口，释放资源。
│   │     └── strategy.py · DefaultMemoryProvider.shutdown()
│   │           ├── store.py · hasattr(store, "shutdown")（不实现 → 跳过）
│   │           ├── retriever.py · hasattr(retriever, "shutdown")（不实现 → 跳过）
│   │           └── adapters/vector_store.py/graph_store.py（若实现：unload model / 关连接）
│   │
│   ├── set_memory_provider(provider) —— 注入
│   │     测试用，直接设为当前实例。无项目内引用。
│   │
│   ├── start_memory_worker(manager, llm) —— 启动 worker
│   │     启动 daemon 线程，异步处理沉淀。幂等。
│   │     ├── worker.py · MemoryWorker(manager=, llm=)（创建实例）
│   │     │     └── manager.py（worker 内部调四操作）
│   │     └── worker.py · worker.start()（启线程）
│   │           └── _run()（消费队列）
│   │
│   ├── shutdown_memory_worker(timeout) —— 关 worker
│   │     drain 队列 + 关线程。
│   │     └── worker.py · worker.shutdown(timeout=)
│   │
│   └── get_memory_worker() —— 取 worker
│         返回当前 worker（可能为 None）。无项目内引用。
│
│
├── 主链② MemoryMiddleware —— 召回（读路径）
│   │   每轮对话开始前召回并注入，结束后清 turn_id。
│   │
│   ├── abefore_model(state, runtime) —— 召回 + 注入
│   │   │   提取 query → 检索 → 预算裁剪 → 注入 HumanMessage + 注入 turn_id。
│   │   │
│   │   ├── state/types.py · state.get("messages")/state.get("thread_id")
│   │   │     读对话历史取最后一条用户消息；取 thread_id 拼 turn_id。
│   │   │
│   │   ├── manager.py · set_turn_id(turn_id)
│   │   │     注入 ContextVar，让 manager 后续操作能取到 actor。
│   │   │
│   │   ├── retriever.py · provider.retriever().retrieve(MemoryQuery)
│   │   │     真正召回。
│   │   │     ├── store.py · list_all()（取候选）
│   │   │     ├── decay.py · compute_strength()（lazy decay 算强度）
│   │   │     ├── schema.py · MemoryTrace.with_strength()（派生强化 trace）
│   │   │     ├── store.py · update()（强化写回）
│   │   │     │     └── bootstrap.py · _wrap_store → retriever.on_trace_updated()（同步索引）
│   │   │     └── types.py · RetrievalResult.compute_score()（复合分数）
│   │   │
│   │   ├── types.py · MemoryQuery(text=query)（构造检索入参）
│   │   │
│   │   ├── manager.py · set_turn_id(None)（无召回时清除）
│   │   │
│   │   └── schema.py / types.py · RetrievalResult.score/.trace.content（格式化文本）
│   │
│   └── aafter_model(state, runtime) —— 清除
│         无条件清 turn_id，抽取交给主链③。
│         └── manager.py · set_turn_id(None)
│
│
├── 主链③ MemoryConsolidationMiddleware —— 沉淀触发（写路径）
│   │   每 N 个用户轮次，构造沉淀任务丢给 worker，立即返回不阻塞。
│   │
│   ├── abefore_model(state, runtime) —— no-op
│   │     沉淀只在 after_model。无项目内引用。
│   │
│   └── aafter_model(state, runtime) —— 每 N 轮 submit
│       │   数用户轮次，到 N 倍数就取最近 N*2 条消息，构造任务丢 worker。
│       ├── state/types.py · state.get("messages")/state.get("thread_id")（数轮次、取 thread_id）
│       ├── worker.py · MemoryTask(thread_id, messages, turn_count)（构造任务）
│       └── worker.py · worker.submit(task)（非阻塞入队）
│             └── self._queue.put(task)
│
│
├── 后台 worker.py —— MemoryWorker
│   │   daemon 线程消费队列，异步做「抽取 episodic + 合并 semantic」。
│   │
│   ├── start() —— 启动线程
│   │     └── _run()（消费循环）
│   │
│   ├── submit(task) —— 丢任务
│   │     └── self._queue.put(task)
│   │
│   ├── shutdown(timeout) —— 关闭
│   │
│   ├── _run() —— 消费循环
│   │     循环取任务，交 _process，异常不崩。
│   │     └── _process(task)
│   │        ├── manager.py · set_turn_id("worker:...")（标 actor）
│   │        │
│   │        ├── _extract_and_encode(task)
│   │        │     ├── manager.py · self._manager.encode(...)（落库）
│   │        │     │     ├── store.py · get（幂等检查）/add（查重 + 落库）
│   │        │     │     │     └── bootstrap.py · _wrap_store → retriever.on_trace_added()（同步索引）
│   │        │     │     ├── schema.py · MemoryTrace/OperationLog（构造 + 记日志）
│   │        │     │     └── journal · emit("memory.encode")（发事件）
│   │        │     └── schema.py · MemoryType(...)（类型字符串转枚举）
│   │        │
│   │        ├── _maybe_consolidate(task)
│   │        │     ├── config.py · get_memory_config()（读阈值）
│   │        │     ├── store.py · manager._store.list_by_type(EPISODIC)（查总量）
│   │        │     └── manager.py · self._manager.consolidate(ids, merged)（合并）
│   │        │           ├── store.py · get/add/batch_update（读旧写新标旧）
│   │        │           │     └── bootstrap.py · _wrap_store → retriever.on_trace_added/updated（同步索引）
│   │        │           ├── schema.py · with_operation（记日志）
│   │        │           └── journal · emit("memory.consolidate")（发事件）
│   │        │
│   │        └── manager.py · finally: set_turn_id(None)（清理 actor）
│   │
│   └── _format_messages(messages) —— 格式化
│         把 messages 拼成 LLM 输入文本。无项目内引用。
│
│
└── 横向引用（主链之外的文件互相引）
    │
    ├── strategies/default/strategy.py —— 装配
    │     真正把 store / retriever / manager / 策略拼成 provider。
    │     ├── config.py · get_memory_config()（取装配参数）
    │     ├── decay.py · EbbinghausDecayPolicy()（衰减策略）
    │     │     ├── config.py（读参数）
    │     │     └── _constants.py（缺省参数）
    │     ├── forget.py · CompositeForgetPolicy(decay)（遗忘策略）
    │     │     ├── decay.py
    │     │     ├── config.py
    │     │     └── _constants.py
    │     ├── store.py · MarkdownFileStore(storage_path)（持久化）
    │     │     ├── exceptions.py
    │     │     ├── schema.py
    │     │     └── types.py
    │     ├── retriever.py · HybridRetriever(store, decay)（检索）
    │     │     ├── memory_store.py / retriever.py（Protocol）
    │     │     ├── schema.py
    │     │     ├── _constants.py（BM25 参数）
    │     │     ├── decay.py
    │     │     └── types.py
    │     └── manager.py · DefaultMemoryManager(...)（四操作编排）
    │
    ├── strategies/default/decay.py —— 衰减
    │     纯计算当前强度（Ebbinghaus + 访问强化 + 重要性）。
    │     ├── config.py · get_memory_config()（每次取最新参数）
    │     ├── schema.py · MemoryTrace/MemoryType（读 trace 字段）
    │     └── _constants.py · DECAY_COEFFICIENTS/DECAY_PARAMS（缺省回退）
    │
    ├── strategies/default/forget.py —— 遗忘
    │     判断是否该遗忘（TTL 过期 或 strength 太低）。
    │     ├── config.py · get_memory_config()（读阈值）
    │     ├── decay.py · EbbinghausDecayPolicy（规则 2 算强度）
    │     ├── schema.py · MemoryTrace（读 last_accessed/created_at）
    │     └── _constants.py · FORGET_THRESHOLDS（缺省回退）
    │
    ├── strategies/default/manager.py —— 四操作
    │     编排 encode / associate / consolidate / reconsolidate。
    │     ├── config.py · get_memory_config()
    │     ├── exceptions.py · MemoryNotFoundError
    │     ├── memory_store.py · MemoryStore
    │     ├── schema.py · Association/MemoryTrace/MemoryType/OperationLog
    │     ├── _constants.py · ASSOCIATE_DEFAULTS/CONSOLIDATE_PARAMS/DECAY_PARAMS
    │     ├── decay.py / forget.py（策略实例）
    │     ├── store.py · get/add/update/batch_update（落 store）
    │     ├── bootstrap.py · _wrap_store（间接，同步索引）
    │     └── journal · emit（发事件）
    │
    ├── strategies/default/store.py —— 持久化
    │     Markdown traces.md 作 truth source，内存索引派生。
    │     ├── exceptions.py · MemoryConflictError/MemoryNotFoundError
    │     ├── schema.py · Association/MemoryTrace/MemoryType/OperationLog
    │     └── types.py · MemoryFilter
    │
    ├── strategies/default/retriever.py —— 检索
    │     BM25 召回 + 强化写回 + forgotten 过滤 + 增量索引。
    │     ├── memory_store.py · MemoryStore
    │     ├── retriever.py · Retriever
    │     ├── schema.py · MemoryTrace
    │     ├── _constants.py · BM25_PARAMS
    │     ├── decay.py · EbbinghausDecayPolicy
    │     ├── types.py · MemoryQuery/RetrievalResult
    │     ├── store.py · list_all()/update()
    │     └── bootstrap.py · _wrap_store（间接，被回调 on_trace_*）
    │
    └── adapters/vector_store.py / graph_store.py —— 空壳
          定义可选叠加索引契约（向量检索 / 图扩散），当前未接线。
          ├── schema.py · MemoryTrace（输入类型）
          └── 当前 HybridRetriever 未 import，属 Layer 6 预留
```



```
bootstrap.py      负责「装配」：把契约+实现+config 拼成 provider，管生死。
MemoryMiddleware  负责「读」：每轮召回 + 注入 + 清 turn_id。
ConsolidationMW   负责「写」：每 N 轮丢任务给 worker，异步沉淀。
```

三者串起来就是 memory 的**主链**：

```
bootstrap 装配 provider
    → MemoryMiddleware 每轮读（retrieve + 注入）
    → ConsolidationMiddleware 每 N 轮写（submit → worker → encode/consolidate）
    → 写回 store → _wrap_store 同步 retriever → 下一轮读得到新记忆（闭环）
```


---

# 1. 引用追溯表

## bootstrap.py

| 引用点                        | 被引项目文件                                           | 被引点再往下                          | 终点      |
| -------------------------- | ------------------------------------------------ | ------------------------------- | ------- |
| `get_memory_config()`      | `config.py`                                      | `_memory_config`                | 叶子（数据类） |
| `_make_journal_callback`   | `agents/journal`                                 | `RunJournal.append`             | 叶子      |
| `build_default_provider`   | `strategies/default/strategy.py`                 | 见下                              | —       |
| `_wrap_store`              | `store.py` + `retriever.py`                      | `on_trace_*`                    | 叶子      |
| `MemoryWorker`             | `worker.py`                                      | 见下                              | —       |
| `shutdown_memory_provider` | `strategy.py` → `DefaultMemoryProvider.shutdown` | `store/retriever.shutdown`（不实现） | 叶子      |

## strategies/default/strategy.py

| 引用点 | 被引项目文件 | 被引点再往下 | 终点 |
|---|---|---|---|
| `get_memory_config` | `config.py` | — | 叶子 |
| `EbbinghausDecayPolicy` | `decay.py` | `get_memory_config` + `_constants` | 叶子 |
| `CompositeForgetPolicy` | `forget.py` | `decay.py` + `_constants` + `config.py` | 叶子 |
| `MarkdownFileStore` | `store.py` | `schema.py` + `types.py` + `exceptions.py` | 叶子 |
| `HybridRetriever` | `retriever.py` | `store.py` + `decay.py` + `schema.py` + `types.py` + `_constants.py` | 叶子 |
| `DefaultMemoryManager` | `manager.py` | `store.py` + `decay.py` + `forget.py` + `schema.py` + `_constants.py` + `exceptions.py` + `config.py` | 叶子 |

## strategies/default/manager.py（四操作内部）

| 操作 | 引用点 | 被引项目文件 | 终点 |
|---|---|---|---|
| `encode` | `_compute_trace_id` | 本文件（内部） | 叶子 |
| | `store.get` / `store.add` | `store.py` | 叶子 |
| | `MemoryTrace` / `OperationLog` | `schema.py` | 叶子 |
| | `_emit_journal` | 本文件（内部） | 叶子 |
| `associate` | `store.get` / `store.update` | `store.py` | 叶子 |
| | `_add_association_with_lru` | 本文件（内部） | 叶子 |
| | `Association` / `with_operation` | `schema.py` | 叶子 |
| `consolidate` | `store.get` / `store.add` / `store.batch_update` | `store.py` | 叶子 |
| | `MemoryTrace` / `OperationLog` | `schema.py` | 叶子 |
| | `_compute_trace_id` | 本文件（内部） | 叶子 |
| `reconsolidate` | `store.get` / `store.update` | `store.py` | 叶子 |
| | `with_operation` | `schema.py` | 叶子 |
| `set_turn_id` | `_turn_id_var`（本文件 ContextVar） | 本文件（内部） | 叶子 |

## strategies/default/store.py（读写内部）

| 引用点 | 被引项目文件 | 终点 |
|---|---|---|
| `_load` → `_parse_trace` | `schema.py`（MemoryType / Association / OperationLog / MemoryTrace） | 叶子 |
| `_serialize_trace` | `schema.py`（字段读取） | 叶子 |
| `add` / `update` / `batch_update` / `remove` | `exceptions.py`（Conflict / NotFound） | 叶子 |
| `list_by_filter` | `types.py`（MemoryFilter） | 叶子 |
| `_diff_to_serializable` | 本文件（内部） | 叶子 |

## strategies/default/retriever.py（检索内部）

| 引用点 | 被引项目文件 | 终点 |
|---|---|---|
| `__init__` → `_build_index_from_store` | `store.py`（`list_all`） | 叶子 |
| `_index_trace` / `_remove_trace_from_index` | 本文件（内部） | 叶子 |
| `retrieve` → `store.list_all` / `store.update` | `store.py` | 叶子 |
| `retrieve` → `decay_policy.compute_strength` | `decay.py` | 叶子 |
| `retrieve` → `MemoryTrace.with_strength` | `schema.py` | 叶子 |
| `retrieve` → `RetrievalResult.compute_score` | `types.py` | 叶子 |
| `on_trace_added/updated/removed` | 本文件（内部） | 叶子 |

## 两个中间件

| 引用点 | 被引项目文件 | 终点 |
|---|---|---|
| `_extract_query` / `_build_turn_id` | `state/types.py`（ThreadState） | 叶子 |
| `set_turn_id` | `strategies/default/manager.py` | 叶子 |
| `provider.retriever().retrieve` | `strategies/default/retriever.py` | 叶子 |
| `MemoryQuery` / `RetrievalResult` | `types.py` | 叶子 |
| `MemoryTask` / `worker.submit` | `worker.py` | 叶子 |
| `state.get(...)` | `state/types.py`（ThreadState） | 叶子 |

## worker.py

| 引用点 | 被引项目文件 | 终点 |
|---|---|---|
| `_process` → `set_turn_id` | `strategies/default/manager.py` | 叶子 |
| `_extract_and_encode` → `manager.encode` | `strategies/default/manager.py` | 叶子 |
| `_maybe_consolidate` → `get_memory_config` | `config.py` | 叶子 |
| `_maybe_consolidate` → `manager._store.list_by_type` | `strategies/default/store.py` | 叶子 |
| `_maybe_consolidate` → `manager.consolidate` | `strategies/default/manager.py` | 叶子 |
| `MemoryType` | `schema.py` | 叶子 |

## adapters/（当前空壳）

| 引用点 | 被引项目文件 | 终点 |
|---|---|---|
| `VectorStore` Protocol | `schema.py`（MemoryTrace） | 叶子 |
| `GraphStore` Protocol | `schema.py`（MemoryTrace） | 叶子 |
| 被谁引用 | **当前 HybridRetriever 未 import adapters** → 空壳保留 Layer 6 | 叶子 |

---

| 叶子类型 | 具体文件 | 说明 |
|---|---|---|
| **数据模型** | `schema.py`、`types.py` | dataclass / Enum，不再往下引项目文件 |
| **常量** | `_constants.py` | 纯字典常量 |
| **异常** | `exceptions.py` | 异常类，无项目内引用 |
| **契约** | `memory_store.py`、`retriever.py`、`memory_manager.py`、`memory_provider.py`、`decay_policy.py`、`forget_policy.py`、`persona_policy.py` | Protocol，零实现 |
| **外部未提供** | `agents/journal`（`get_run_journal` / `RunJournal.append`） | 项目内但源码未提供，标为叶子 |
| **外部注入** | `llm`、`runtime`、`state` | 由调用方 / LangGraph 提供 |
| **adapters 空壳** | `adapters/vector_store.py`、`adapters/graph_store.py` | 当前未被 HybridRetriever import，纯契约叶子 |


下面这版是**结合当前全部源码后的优化引用图**。相比你手上那版，主要修正/增强了几点：

1. **分层更清楚**：把「启动装配层 → 实现层 → 接入层」分成三段，不再混在一起。
2. **方向纠错**：`manager → store → retriever` 这条链之前画反了（`manager` 依赖 `store`/`retriever`，但 `store` 不依赖 `retriever`，`retriever` 依赖 `store`）。现在按真实依赖画。
3. **`adapters/` 标注为「空壳未接线」**：当前 `HybridRetriever` 没 import adapter，画成虚线预留，不误导成已接通。
4. **`journal/` 从「旁路」提升为「横切关注点」**：它被 manager 通过 callback 注入，画成贯穿线而不是孤立节点。
5. **`_wrap_store` 闭环显式画出**：这是读写一致的关键，单独作为一条回边。
6. **两个中间件的「同轮顺序」显式表达**：它们互不 import，靠 after_model 顺序串联。
7. **config 的横切属性**：`get_memory_config()` 被 decay/forget/manager/strategy/worker 多处调用，画成全局横切。

---

# 2. Memory 引用图

## 2.1 启动装配层（进程级，一次性）

```
                        ┌─────────────────────────────┐
                        │        bootstrap.py         │
                        │  装配 + 生命周期 + worker.    │
                        │                             │
                        │  get/reset/shutdown/set_    │
                        │  memory_provider()          │
                        │  start/shutdown/get_        │
                        │  memory_worker()            │
                        └──────────────┬──────────────┘
                                       │
              ┌────────────────────────┼────────────────────────┐
              │                        │                        │
              ▼                        ▼                        ▼
      ┌──────────────┐        ┌──────────────┐        ┌──────────────────┐
      │  config.py   │        │  worker.py   │        │ strategies/      │
      │  全局单例     │        │ MemoryWorker │        │ default/         │
      │              │        │              │        │ strategy.py      │
      │ get/set_     │        │ start/submit │        │ build_default_   │
      │ memory_config│        │ /shutdown    │        │ provider()       │
      └──────┬───────┘        └──────┬───────┘        └────────┬─────────┘
             │                       │                         │
             │                       │                         │
             │  ┌────────────────────┘                         │
             │  │                                              │
             │  │   ┌──────────────────────────────────────────┘
             │  │   │
             ▼  ▼   ▼
      ┌──────────────────────────────────────────────────────────────┐
      │                      实现层（strategies/default/）            │
      │                                                              │
      │   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐    │
      │   │  decay.py    │   │  forget.py   │   │  manager.py  │    │
      │   │ Ebbinghaus   │◀──│  Composite   │   │   Default    │    │
      │   │ DecayPolicy  │   │ ForgetPolicy │   │ MemoryManager│    │
      │   └──────┬───────┘   └──────┬───────┘   │  + set_turn_ │    │
      │          │                  │           │  id()        │    │
      │          │                  │           └──────┬───────┘    │
      │          │                  │                  │            │
      │          │                  │                  │            │
      │          │                  │                  ▼            │
      │          │                  │           ┌──────────────┐    │
      │          │                  │           │  store.py    │    │
      │          │                  │           │  Markdown    │    │
      │          │                  │           │  FileStore   │    │
      │          │                  │           └──────┬───────┘    │
      │          │                  │                  │            │
      │          │                  │                  ▼            │
      │          │                  │           ┌──────────────┐    │
      │          │                  └──────────▶│ retriever.py │    │
      │          │                              │   Hybrid     │    │
      │          └─────────────────────────────▶│  Retriever   │    │
      │                                         └──────┬───────┘    │
      │                                                │            │
      │                                                ┆ 虚线：      │
      │                                                ┆ 当前未接线   │
      │                                                ▼            │
      │                                         ┌──────────────┐    │
      │                                         │  adapters/   │    │
      │                                         │ Vector/Graph │    │
      │                                         │  (Layer 6)   │    │
      │                                         └──────────────┘    │
      └──────────────────────────────────────────────────────────────┘
                                       │
                                       │  DefaultMemoryProvider
                                       │  (store + retriever + manager)
                                       ▼

                              （透传给接入层）

【横向：数据底座（被实现层全部引用）】
      ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
      │  schema.py   │   │  types.py    │   │_constants.py │
      │ MemoryTrace  │   │ MemoryQuery  │   │  常量字典     │
      │ MemoryType   │   │ MemoryFilter │   │              │
      │ OperationLog │   │ Retrieval    │   │              │
      │ Association  │   │ Result       │   │              │
      └──────────────┘   └──────────────┘   └──────────────┘
             ▲                  ▲                  ▲
             └──────────────────┴──────────────────┘
                    被 decay/forget/manager/store/
                    retriever/strategy 全部引用

【横向：异常底座】
      ┌──────────────┐
      │ exceptions.py│
      │  MemoryError │
      │  + 5 子类     │
      └──────────────┘
             ▲
             └─ 被 store.py / manager.py 引用
```

---

## 2.2 接入层（Agent 链，运行时）

```
┌──────────────────────────────────────────────────────────────────────┐
│                          Agent 链（按挂载顺序）                        │
│                                                                      │
│   SandboxMiddleware                                                  │
│        │                                                             │
│        ▼                                                             │
│   ┌─────────────────────────────┐                                    │
│   │   MemoryMiddleware          │  ← 读路径（召回）                   │
│   │   （memory_recall_          │                                    │
│   │     middleware.py）         │                                    │
│   │                             │                                    │
│   │  abefore_model:             │                                    │
│   │   _extract_query            │                                    │
│   │   _build_turn_id            │                                    │
│   │   set_turn_id ──────────────┼──┐                                 │
│   │   provider.retriever()      │  │                                 │
│   │     .retrieve()             │  │                                 │
│   │   _format_recall            │  │                                 │
│   │                             │  │                                 │
│   │  aafter_model:              │  │                                 │
│   │   set_turn_id(None) ────────┼──┤                                 │
│   └─────────────────────────────┘  │                                 │
│        │                           │                                 │
│        ▼                           │                                 │
│   ┌─────────────────────────────┐  │                                 │
│   │ MemoryConsolidationMW       │  │                                 │
│   │ （memory_consolidation_     │  │                                 │
│   │   middleware.py）           │  │                                 │
│   │                             │  │                                 │
│   │  abefore_model: no-op       │  │                                 │
│   │                             │  │                                 │
│   │  aafter_model:              │  │                                 │
│   │   计数 HumanMessage          │  │                                 │
│   │   % N == 0 时:              │  │                                 │
│   │   worker.submit(task) ──────┼──┼──┐                              │
│   └─────────────────────────────┘  │  │                              │
│        │                           │  │                              │
│        ▼                           │  │                              │
│   HelpRequestMiddleware            │  │                              │
│        │                           │  │                              │
│        ▼                           │  │                              │
│   ToolCallMiddleware               │  │                              │
│                                    │  │                              │
└────────────────────────────────────┼──┼──────────────────────────────┘
                                     │  │
              ┌──────────────────────┘  │
              │                         │
              ▼                         ▼
      ┌──────────────┐          ┌──────────────┐
      │ manager.py   │          │  worker.py   │
      │ set_turn_id  │          │ MemoryWorker │
      │ (ContextVar) │          │              │
      └──────────────┘          │  _run()      │
                                │  _process()  │
                                └──────┬───────┘
                                       │
                                       │ 内部调 manager 四操作
                                       ▼
                                ┌──────────────┐
                                │ manager.py   │
                                │ encode /     │
                                │ consolidate  │
                                └──────┬───────┘
                                       │
                                       ▼
                                ┌──────────────┐
                                │  store.py    │
                                │ Markdown     │
                                │ FileStore    │
                                └──────────────┘
```

**两个中间件的关系**：
- 互不 import，靠 **同一轮 `aafter_model` 的挂载顺序**串联（MemoryMiddleware 先、ConsolidationMW 后）。
- 共享同一个 provider / worker（由 bootstrap 装配、make_lead_agent 透传）。

---

## 2.3 闭环回边（读写一致的关键）

```
     【写路径】                              【读路径】
  ConsolidationMW                        MemoryMiddleware
        │                                      │
        ▼                                      ▼
   worker._process                        retrieve()
        │                                      │
        ▼                                      │
   manager.encode/consolidate                  │
        │                                      │
        ▼                                      │
     store.add / update / batch_update         │
        │                                      │
        │                                      │
        └──────────► _wrap_store ◄─────────────┘
                   （bootstrap 装配时挂载）
                        │
                        │ store 变更后回调
                        ▼
              retriever.on_trace_added
              retriever.on_trace_updated
              retriever.on_trace_removed
                        │
                        │ 增量维护 BM25 倒排索引
                        ▼
                 HybridRetriever 索引
                        │
                        │ 下一轮 retrieve 立即可见
                        ▼
              （回到 MemoryMiddleware 读路径）
```

**闭环点一句话**：`_wrap_store` 把 store 的写操作同步到 retriever 的增量索引，保证「本轮沉淀的记忆，下一轮召回就能看到」。

---

## 2.4 完整数据流（一图串起三层）

```
【进程启动】
  bootstrap.get_memory_provider()
      │
      ├─ config.get_memory_config()          ← config.py
      ├─ build_default_provider()             ← strategy.py
      │     ├─ decay.py / forget.py
      │     ├─ store.py（MarkdownFileStore）
      │     ├─ retriever.py（HybridRetriever，空壳接 adapters/）
      │     └─ manager.py（DefaultMemoryManager + set_turn_id）
      └─ _wrap_store(store, retriever)        ← 闭环挂载
      │
      ▼
  make_lead_agent（5 处透传 provider）
      │
      ├─ MemoryMiddleware(provider)
      └─ ConsolidationMW(worker)
              │
              ▼
【每轮对话】
  abefore_model（MemoryMiddleware）
      ├─ set_turn_id(turn_id)                 ← manager.py
      ├─ retriever.retrieve()
      │     ├─ store.list_all()                ← store.py
      │     ├─ decay.compute_strength()        ← decay.py
      │     ├─ trace.with_strength()           ← schema.py
      │     ├─ store.update()                  ← store.py
      │     │     └─ _wrap_store → retriever.on_trace_updated()
      │     └─ RetrievalResult.compute_score() ← types.py
      └─ 注入 memory_recall HumanMessage
              │
              ▼
  Leader Agent 跑（工具调用 / 沙箱 / 子 Agent）
              │
              ▼
  aafter_model（MemoryMiddleware）
      └─ set_turn_id(None)                    ← manager.py
              │
              ▼
  aafter_model（ConsolidationMW）
      └─ worker.submit(task)                  ← worker.py
              │
              ▼（异步）
  worker._process
      ├─ set_turn_id("worker:...")            ← manager.py
      ├─ manager.encode()                     ← manager.py
      │     └─ store.add() → _wrap_store → retriever.on_trace_added()
      ├─ manager.consolidate()                ← manager.py
      │     ├─ store.add() → _wrap_store → retriever.on_trace_added()
      │     └─ store.batch_update() → _wrap_store → retriever.on_trace_updated()
      └─ finally: set_turn_id(None)           ← manager.py
              │
              ▼
  store.py（traces.md truth source）
              │
              ▼
  _wrap_store 同步 retriever 索引
              │
              ▼
  （下一轮 retrieve 可见）
```

---

## 2.5 总结

```
启动装配层：bootstrap → strategy → (store + retriever + manager + policies)
接入层：    MemoryMiddleware（读） + ConsolidationMW（写）
闭环：      _wrap_store 把写同步到读索引
横切：      config.py（全局配置）+ schema/types/_constants（数据底座）
预留：      adapters/（Layer 6）+ journal/（事件旁路）
```



# 3. 结构树
```
poirot/backend/agents/memory/
│
├── 【契约层】Protocol + 异常（零实现，被实现层实现，被主链约束）
│   │
│   ├── memory_store.py —— MemoryStore Protocol
│   │   │   约束：持久化后端必须提供 8 个方法
│   │   │
│   │   ├── 定义的方法（被 MarkdownFileStore 实现）
│   │   │   ├── add(trace)                      ← 新增，id 冲突抛 MemoryConflictError
│   │   │   ├── get(trace_id)                   ← 按 id 取，不存在返 None
│   │   │   ├── update(trace)                   ← 替换，id 不存在抛 MemoryNotFoundError
│   │   │   ├── batch_update(traces)            ← 批量替换（consolidate 标 forgotten 用）
│   │   │   ├── remove(trace_id)                ← 删除，幂等
│   │   │   ├── list_by_type(type)              ← 按类型列出
│   │   │   ├── list_by_filter(filter)          ← 按过滤器列出
│   │   │   └── list_all()                      ← 列出全部
│   │   │
│   │   ├── 引用的类型
│   │   │   ├── schema.py · MemoryTrace / MemoryType
│   │   │   └── types.py · MemoryFilter
│   │   │
│   │   └── 被谁实现 / 引用
│   │       ├── strategies/default/store.py · MarkdownFileStore（实现）
│   │       ├── strategies/default/manager.py · DefaultMemoryManager（构造参数）
│   │       ├── strategies/default/retriever.py · HybridRetriever（构造参数）
│   │       └── strategies/default/strategy.py · build_default_provider（装配注入）
│   │
│   ├── retriever.py —— Retriever Protocol
│   │   │   约束：检索后端必须提供 retrieve 方法
│   │   │
│   │   ├── 定义的方法（被 HybridRetriever 实现）
│   │   │   └── retrieve(query) → list[RetrievalResult]
│   │   │         复合分数：similarity × 0.7 + strength × 0.3
│   │   │         命中后自动强化（lazy decay + access_count+1）
│   │   │
│   │   ├── 引用的类型
│   │   │   └── types.py · MemoryQuery / RetrievalResult
│   │   │
│   │   └── 被谁实现 / 引用
│   │       ├── strategies/default/retriever.py · HybridRetriever（实现）
│   │       ├── strategies/default/strategy.py · build_default_provider（注入 provider）
│   │       └── memory_recall_middleware.py · abefore_model（provider.retriever().retrieve()）
│   │
│   ├── memory_manager.py —— MemoryManager Protocol
│   │   │   约束：四操作编排必须提供 4 个方法，且不含 LLM
│   │   │
│   │   ├── 定义的方法（被 DefaultMemoryManager 实现）
│   │   │   ├── encode(content, type, *, importance, source, metadata) → MemoryTrace
│   │   │   ├── associate(trace_id_a, trace_id_b, *, strength, type) → None
│   │   │   ├── consolidate(trace_ids, merged_content) → MemoryTrace
│   │   │   └── reconsolidate(trace_id, new_content) → MemoryTrace
│   │   │   注意：没有 retrieve —— 检索统一走 Retriever
│   │   │
│   │   ├── 引用的类型
│   │   │   └── schema.py · MemoryTrace / MemoryType
│   │   │
│   │   └── 被谁实现 / 引用
│   │       ├── strategies/default/manager.py · DefaultMemoryManager（实现）
│   │       ├── strategies/default/strategy.py · build_default_provider（注入 provider）
│   │       └── worker.py · _process（self._manager.encode / consolidate）
│   │
│   ├── memory_provider.py —— MemoryProvider Protocol
│   │   │   约束：组合门面必须提供 3 个取子组件方法
│   │   │
│   │   ├── 定义的方法（被 DefaultMemoryProvider 实现）
│   │   │   ├── store() → MemoryStore
│   │   │   ├── retriever() → Retriever
│   │   │   └── manager() → MemoryManager
│   │   │   注意：Protocol 不定义 shutdown —— 走 hasattr duck-type
│   │   │
│   │   ├── 引用的类型
│   │   │   ├── memory_store.py · MemoryStore
│   │   │   ├── retriever.py · Retriever
│   │   │   └── memory_manager.py · MemoryManager
│   │   │
│   │   └── 被谁实现 / 引用
│   │       ├── strategies/default/strategy.py · DefaultMemoryProvider（实现）
│   │       ├── bootstrap.py · get_memory_provider（返回类型）
│   │       └── 两个中间件（构造时接收）
│   │
│   ├── decay_policy.py —— DecayPolicy Protocol
│   │   │   约束：衰减策略必须提供 compute_strength 方法
│   │   │
│   │   ├── 定义的方法（被 EbbinghausDecayPolicy 实现）
│   │   │   └── compute_strength(trace, now) → float（0.0~1.0）
│   │   │         lazy decay：不修改 trace，retrieve 时由 Retriever 调用
│   │   │
│   │   ├── 引用的类型
│   │   │   └── schema.py · MemoryTrace
│   │   │
│   │   └── 被谁实现 / 引用
│   │       ├── strategies/default/decay.py · EbbinghausDecayPolicy（实现）
│   │       ├── strategies/default/forget.py · CompositeForgetPolicy（构造接收）
│   │       ├── strategies/default/manager.py · DefaultMemoryManager（构造接收）
│   │       └── strategies/default/retriever.py · HybridRetriever（构造接收）
│   │
│   ├── forget_policy.py —— ForgetPolicy Protocol
│   │   │   约束：遗忘策略必须提供 should_forget 方法，且不含矛盾解决
│   │   │
│   │   ├── 定义的方法（被 CompositeForgetPolicy 实现）
│   │   │   └── should_forget(trace, now) → bool
│   │   │         TTL 过期 或 strength 低于阈值 → True
│   │   │
│   │   ├── 引用的类型
│   │   │   └── schema.py · MemoryTrace
│   │   │
│   │   └── 被谁实现 / 引用
│   │       ├── strategies/default/forget.py · CompositeForgetPolicy（实现）
│   │       └── strategies/default/manager.py · DefaultMemoryManager（构造接收）
│   │
│   ├── persona_policy.py —— PersonaPolicy Protocol
│   │   │   约束：用户画像策略必须提供 3 个方法（当前无实现，L6 预留）
│   │   │
│   │   ├── 定义的方法
│   │   │   ├── get_static_profile(user_id) → dict      ← 稳定事实
│   │   │   ├── get_dynamic_profile(user_id) → dict     ← 近期活动
│   │   │   └── update_profile(user_id, facts) → None   ← 从 semantic 归纳
│   │   │
│   │   ├── 引用的类型
│   │   │   └── （无项目内引用）
│   │   │
│   │   └── 被谁实现 / 引用
│   │       └── 当前无实现、无引用（Layer 6 预留 StaticDynamicPersona）
│   │
│   └── exceptions.py —— 异常层次
│       │   约束：错误信号必须带 details，fail-closed
│       │
│       ├── 定义的类
│       │   ├── MemoryError（基类，带 details: dict + __str__ 自动展开）
│       │   ├── MemoryNotFoundError(trace_id)                   ← 找不到 trace
│       │   ├── MemoryStoreError(message, *, operation, path)   ← 存储失败
│       │   ├── MemoryRetrieveError(message, *, query)          ← 检索失败（query 截断）
│       │   ├── MemoryConsolidateError(message, *, trace_ids)   ← 巩固失败
│       │   └── MemoryConflictError(message, *, old_id, new_id) ← 矛盾冲突
│       │
│       └── 被谁引用 / 抛出
│           ├── strategies/default/store.py
│           │     ├── add 冲突 → MemoryConflictError
│       │     └── update 找不到 → MemoryNotFoundError
│       └── strategies/default/manager.py
│             └── associate / reconsolidate 找不到 → MemoryNotFoundError
│
│
├── 【实现层】strategies/default/（满足契约，被装配层引用）
│   │
│   ├── _constants.py —— 默认策略常量
│   │   ├── DECAY_PARAMS              ← 三类记忆衰减参数
│   │   ├── RETRIEVAL_WEIGHTS         ← 复合检索分数权重
│   │   ├── DECAY_COEFFICIENTS        ← 衰减公式系数
│   │   ├── FORGET_THRESHOLDS         ← 遗忘阈值
│   │   ├── CONSOLIDATE_PARAMS        ← 巩固参数
│   │   ├── ASSOCIATE_DEFAULTS        ← 关联默认
│   │   └── BM25_PARAMS               ← BM25 检索参数
│   │
│   ├── decay.py —— EbbinghausDecayPolicy
│   │   │   实现 DecayPolicy Protocol
│   │   ├── compute_strength(trace, now) → float
│   │   └── _get_decay_params(type) → dict
│   │
│   ├── forget.py —— CompositeForgetPolicy
│   │   │   实现 ForgetPolicy Protocol
│   │   ├── should_forget(trace, now) → bool
│   │   └── _get_thresholds() → dict
│   │
│   ├── manager.py —— DefaultMemoryManager
│   │   │   实现 MemoryManager Protocol + 提供 set_turn_id
│   │   ├── encode(content, type, *, importance, source, metadata) → MemoryTrace
│   │   ├── associate(trace_id_a, trace_id_b, *, strength, type) → None
│   │   ├── consolidate(trace_ids, merged_content) → MemoryTrace
│   │   ├── reconsolidate(trace_id, new_content) → MemoryTrace
│   │   ├── _compute_trace_id(content, type) → str
│   │   ├── _add_association_with_lru(trace, new_assoc, max_assocs) → MemoryTrace
│   │   ├── _get_actor() → str | None
│   │   ├── _emit_journal(event, payload) → None
│   │   ├── _get_decay_params(type) → dict
│   │   ├── _get_associate_defaults() → dict
│   │   └── _get_consolidate_params() → dict
│   │   └── 模块级：set_turn_id(turn_id) —— ContextVar 注入/清除
│   │
│   ├── store.py —— MarkdownFileStore
│   │   │   实现 MemoryStore Protocol
│   │   ├── add(trace) / get(trace_id) / update(trace) / batch_update(traces) / remove(trace_id)
│   │   ├── list_by_type(type) / list_by_filter(filter) / list_all()
│   │   ├── _resolve_storage_path(storage_path) → Path
│   │   ├── _load() / _parse_trace(trace_id, body) → MemoryTrace | None
│   │   ├── _serialize_trace(trace) → str
│   │   ├── _diff_to_serializable(diff) → dict | None
│   │   ├── _append_to_file(trace) / _rewrite_file()
│   │   └── threading.Lock 保护 update / remove / batch_update
│   │
│   ├── retriever.py —— HybridRetriever
│   │   │   实现 Retriever Protocol
│   │   ├── retrieve(query) → list[RetrievalResult]
│   │   ├── _build_index_from_store() / _index_trace(trace) / _remove_trace_from_index(trace_id)
│   │   ├── _bm25_score(query_tokens, trace_id, avgdl, k1, b) → float
│   │   ├── _default_tokenize(text) → list[str]
│   │   ├── on_trace_added(trace) / on_trace_updated(trace) / on_trace_removed(trace_id)
│   │   └── 倒排索引：_inverted_index / _trace_lengths
│   │
│   └── strategy.py —— build_default_provider + DefaultMemoryProvider
│       │   实现 MemoryProvider Protocol
│       ├── build_default_provider(*, store, retriever, decay_policy, forget_policy, journal)
│       │     → MemoryProvider
│       └── DefaultMemoryProvider（frozen dataclass）
│             ├── store() / retriever() / manager()
│             └── shutdown() —— hasattr duck-type 委托 store/retriever
│
│
├── 【数据底座】被实现层与主链共享
│   │
│   ├── schema.py —— 记忆原子单元与数据类
│   │   ├── MemoryType（Enum：EPISODIC / SEMANTIC / PROCEDURAL）
│   │   ├── Association（frozen：target_id / strength / type）
│   │   ├── OperationLog（frozen：timestamp / operation / actor / diff）
│   │   └── MemoryTrace（frozen）
│   │         ├── 身份：id / content / type
│   │         ├── 强度：strength / base_strength / decay_rate / access_count / last_accessed / importance
│   │         ├── 关联：associations
│   │         ├── 预留：embedding
│   │         ├── 溯源：source / created_at / metadata
│   │         ├── 审计：operation_log（上限 20 条 FIFO）
│   │         ├── with_strength(new_strength, accessed_at) → MemoryTrace
│   │         └── with_operation(log, *, max_log) → MemoryTrace
│   │
│   ├── types.py —— 辅助类型
│   │   ├── MemoryQuery（text / top_k / type_filter / min_strength / metadata_filter）
│   │   ├── MemoryFilter（type_filter / min_strength / max_age_hours / metadata_filter）
│   │   └── RetrievalResult（trace / similarity / strength / score）
│   │         └── compute_score(trace, similarity, strength) → RetrievalResult
│   │
│   └── config.py —— 全局配置单例
│       ├── MemoryConfig（frozen dataclass）
│       │     ├── use / storage_path / enable_recall / enable_extract / token_budget
│       │     ├── decay / forget / phase2（dict）
│       │     └── vector_store / graph_store
│       ├── STARTUP_ONLY_FIELDS（use / storage_path / vector_store / graph_store）
│       ├── get_memory_config() → MemoryConfig
│       └── set_memory_config(config) → None
│
│
├── 【适配层】adapters/（Layer 6 预留，当前未接线）
│   │
│   ├── vector_store.py —— VectorStore Protocol
│   │   ├── upsert(trace) / search(query_embedding, top_k) / remove(trace_id) / rebuild(traces)
│   │   └── 被谁实现：当前无（Layer 6 预留 Chroma / Faiss / Milvus）
│   │
│   ├── graph_store.py —— GraphStore Protocol
│   │   ├── upsert_node(trace) / upsert_edge(a, b, *, strength, type)
│   │   ├── expand(trace_ids, *, max_depth, min_strength) / remove_node(trace_id) / rebuild(traces)
│   │   └── 被谁实现：当前无（Layer 6 预留 Neo4j / NetworkX）
│   │
│   └── 当前状态
│       └── HybridRetriever 未 import adapters（纯 BM25）→ 空壳保留
│
│
├── 【装配层】bootstrap.py（主链①）
│   │
│   ├── Provider lifecycle
│   │   ├── get_memory_provider()          ← 懒加载 + 双检锁 + 反射 config.use
│   │   ├── reset_memory_provider()        ← 清缓存不关闭（测试用）
│   │   ├── shutdown_memory_provider()     ← 关闭 + 清缓存（duck-type 委托）
│   │   └── set_memory_provider(provider)  ← 注入（测试用）
│   │
│   ├── Worker lifecycle（L5）
│   │   ├── start_memory_worker(manager, llm)   ← 启动 daemon 线程，幂等
│   │   ├── shutdown_memory_worker(timeout)     ← drain + 关闭
│   │   └── get_memory_worker()                 ← 取实例
│   │
│   └── 内部辅助
│       ├── _load_memory_provider(config)   ← build_default_provider + journal + _wrap_store
│       ├── _make_journal_callback()        ← RunJournal 可用 → lambda，否则 None
│       └── _wrap_store(store, retriever)   ← 装饰器：store 变更 → retriever.on_trace_*
│
│
├── 【接入层】两个中间件（主链②③）
│   │
│   ├── memory_recall_middleware.py —— MemoryMiddleware（读路径）
│   │   ├── abefore_model(state, runtime)
│   │   │     ├── _extract_query(state) → str
│   │   │     ├── _build_turn_id(state, runtime) → str
│   │   │     ├── set_turn_id(turn_id)
│   │   │     ├── provider.retriever().retrieve(MemoryQuery)
│   │   │     └── _format_recall(results, token_budget) → str
│   │   └── aafter_model(state, runtime)
│   │         └── set_turn_id(None)
│   │
│   └── memory_consolidation_middleware.py —— MemoryConsolidationMiddleware（写路径）
│       ├── abefore_model(state, runtime) → None（no-op）
│       └── aafter_model(state, runtime)
│             ├── 数 HumanMessage 轮次
│             ├── 取最近 N*2 条 messages
│             └── worker.submit(MemoryTask(...))
│
│
└── 【后台】worker.py —— MemoryWorker
    │
    ├── MemoryTask（dataclass：thread_id / messages / turn_count）
    │
    └── MemoryWorker
        ├── start() / submit(task) / shutdown(timeout)
        ├── _run() → _process(task)
        ├── _extract_and_encode(task) → list[str]
        │     └── manager.encode(...) + MemoryType(...)
        ├── _maybe_consolidate(task)
        │     ├── get_memory_config()
        │     ├── manager._store.list_by_type(EPISODIC)
        │     └── manager.consolidate(ids, merged)
        └── _format_messages(messages) → str
```