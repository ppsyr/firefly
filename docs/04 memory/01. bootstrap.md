---

name: Memory bootstrap lifecycle — get / reset / shutdown / set + 反射加载 + journal 注入。

description:

【整体职责】
Memory 运行时主链的「装配与生命周期」节点。
把长期记忆的 provider + worker 从零构建出来，并管住它们的生死：
- 会话开始前：懒加载装配 provider（读 config → build → 装饰 store）。
- 会话进行中：provider 缓存复用，供 MemoryMiddleware（读）与
  MemoryConsolidationMiddleware（写）取用。
- 会话结束后：shutdown 关闭 provider / worker。

上层（app / middleware）一律通过本模块拿 provider，不直接持有全局单例；
worker（L5）的启停也集中在此。装配动作本身下沉到
strategies/default/strategy.py（build_default_provider）。

【主链位置】
bootstrap（本模块，装配）
    → MemoryMiddleware（abefore_model 读：召回 + 注入）
    → MemoryConsolidationMiddleware（aafter_model 写：submit 沉淀任务）
    → MemoryWorker（异步 encode / consolidate）
    → MarkdownFileStore（落盘）
    → _wrap_store 同步 HybridRetriever 索引 → 下一轮读得到新记忆（闭环）

【组成】
1. Provider lifecycle 4 函数（对外主接口）：
   - get_memory_provider()      ：懒加载 + 双检锁 + 反射 config.use。
                                  首次访问触发 _load_memory_provider，
                                  之后缓存于 _memory_provider 复用。
                                  config.use=="" 时返回 None（记忆禁用，主链短路）。
   - reset_memory_provider()    ：清缓存不关闭（测试用）。
   - shutdown_memory_provider() ：关闭 + 清缓存
                                  （hasattr duck-type 委托 store/retriever）。
   - set_memory_provider(p)     ：注入（测试用）。
2. Worker lifecycle（L5）：
   - start_memory_worker(manager, llm) ：启动 daemon 线程 worker，幂等；
                                         llm 由调用方注入（避免反向依赖 app）。
   - shutdown_memory_worker(timeout)   ：drain + 关闭 worker。
   - get_memory_worker()               ：取当前 worker 实例（可能为 None）。
3. 内部辅助：
   - _load_memory_provider ：调 build_default_provider（L3 完整版）+ journal 注入
                             + _wrap_store 装饰（5B 增量索引闭环）。
   - _make_journal_callback：RunJournal 可用时返回 lambda，不可用时返回 None
                             （静默降级，不阻断装配）。
   - _wrap_store           ：装饰器模式，把 store 变更同步到 retriever 索引
                             （主链读写一致的关键粘合剂）。

【挂载/调用顺序（固定）】
get_memory_provider
    → （首次）_load_memory_provider
        → _make_journal_callback                 # journal 注入（可降级 None）
        → build_default_provider(journal=...)    # 真正装配 store/retriever/manager/policies
        → _wrap_store(provider.store(), provider.retriever())  # 5B 索引闭环
    → 返回 provider（缓存于 _memory_provider）

【职责边界】
- 本模块只负责 provider 的「获取 / 重置 / 关闭 / 注入」与 worker 的「启停」，
  不实现 memory 的具体存储、检索、衰减、遗忘逻辑
  （那些在 strategies/default/ 下的 store / retriever / manager / decay / forget）。
- 装配动作本身不在本模块：build_default_provider 在
  strategies/default/strategy.py，本模块只负责调用它 + 注入 journal + 装饰 store。
- journal 注入通过 _make_journal_callback 间接完成；RunJournal 不可用时静默降级
  （返回 None），不阻断 provider 构建（主链对 journal 弱依赖）。
- 5B 增量索引同步由 _wrap_store 以「运行时方法替换」实现，不改 store 类本身；
  这是主链「写路径 → 读路径」的闭环点。

【INVARIANT】
- 懒加载双检锁：get_memory_provider / start_memory_worker 均线程安全。
- shutdown duck-type：hasattr(provider, "shutdown") 委托 store/retriever；
  MarkdownFileStore 不实现 shutdown，SQLiteShadowStore / VectorStore 实现。
- config.use="" 返 None（记忆禁用）。
- runtime 可切：provider 不缓存 config，每次从 get_memory_config() 取最新
  （本模块只在装配时读一次，runtime 切换由 strategies 内部每次取最新保证）。
- import 防火墙：bootstrap.py 不 import app（journal 延迟导入且可降级）。

---

## 0. 结构树

```
bootstrap.py —— Memory 运行时主链的「装配 + 生命周期」节点
│
│   整体职责：把长期记忆的 provider + worker 从零构建出来，并管住它们的生死。
│   上层一律通过本模块拿 provider，不直接持有全局单例。
│   装配动作本身下沉到 strategies/default/strategy.py（build_default_provider）。
│
│   主链位置：
│   bootstrap（装配）→ MemoryMiddleware（读）→ ConsolidationMW（写）
│       → MemoryWorker（异步）→ MarkdownFileStore（落盘）
│       → _wrap_store 同步 retriever 索引 → 下一轮读得到新记忆（闭环）
│
│
├── 模块级状态（两个独立的锁 + 两个实例缓存）
│   │
│   ├── _provider_lock : threading.Lock        ← 保护 _memory_provider 的并发访问
│   ├── _memory_provider : Any = None          ← provider 实例缓存（懒加载后存这）
│   ├── _worker_lock : threading.Lock          ← 保护 _memory_worker 的并发访问
│   └── _memory_worker : Any = None            ← worker 实例缓存（启动后存这）
│
│   说明：provider 与 worker 各自独立的锁与实例，生命周期解耦。
│         原因：worker 依赖 llm，provider 不依赖 llm。
│
│
├── Provider lifecycle（对外主接口 4 函数）
│   │
│   ├── get_memory_provider() —— 拿 provider（主链入口）
│   │   │
│   │   │   用途：对外唯一 provider 入口。首次装配，之后缓存复用；
│   │   │        记忆禁用（config.use==""）时返回 None，主链短路。
│   │   │
│   │   ├── 做什么
│   │   │   ├── 无锁快路径检查 _memory_provider
│   │   │   ├── 有锁复查（双检锁，防并发重复构建）
│   │   │   ├── get_memory_config() 读 config
│   │   │   ├── config.use 为假 → 返回 None
│   │   │   └── 否则 _load_memory_provider(config) → 缓存 → 返回
│   │   │
│   │   └── 引用的模块
│   │       ├── config.py · get_memory_config()    ← 读 use / storage_path / decay / forget
│   │       └── _load_memory_provider()（本文件内部）
│   │
│   ├── reset_memory_provider() —— 清缓存不关闭（测试用）
│   │   │
│   │   │   用途：把缓存置 None，不调 shutdown，便于测试中替换/复用底层资源。
│   │   │
│   │   └── 做什么
│   │       ├── 加 _provider_lock
│   │       └── _memory_provider = None
│   │
│   ├── shutdown_memory_provider() —— 关闭 + 清缓存（主链出口）
│   │   │
│   │   │   用途：会话结束 / 进程退出时释放资源（文件句柄、SQLite 连接、向量模型）。
│   │   │
│   │   ├── 做什么
│   │   │   ├── 加 _provider_lock
│   │   │   ├── _memory_provider 非 None 时：
│   │   │   │     hasattr(provider, "shutdown") → 调 provider.shutdown()
│   │   │   │     否则跳过
│   │   │   └── _memory_provider = None
│   │   │
│   │   └── 引用的模块（duck-type 委托链）
│   │       └── strategies/default/strategy.py · DefaultMemoryProvider.shutdown()
│   │             ├── hasattr(store, "shutdown")     ← MarkdownFileStore 不实现 → 跳过
│   │             ├── hasattr(retriever, "shutdown") ← HybridRetriever 不实现 → 跳过
│   │             └── adapters/vector_store.py / graph_store.py（若实现 → unload / 关连接）
│   │
│   └── set_memory_provider(provider) —— 注入（测试用）
│       │
│       │   用途：单测 / 集成测试替换真实 provider（注入 mock）。
│       │
│       └── 做什么
│           ├── 加 _provider_lock
│           └── _memory_provider = provider
│
│
├── Worker lifecycle（L5，块 C3）
│   │
│   │   说明：worker 是主链「写路径」的执行体。
│   │         ConsolidationMW.aafter_model → worker.submit(task) → worker 异步处理。
│   │
│   ├── start_memory_worker(manager, llm) —— 启动 worker
│   │   │
│   │   │   用途：启动 daemon 线程，异步处理沉淀任务。幂等。
│   │   │        llm 由调用方注入，避免 worker 反向依赖 app。
│   │   │
│   │   ├── 做什么
│   │   │   ├── 无锁快路径检查 _memory_worker（幂等）
│   │   │   ├── 有锁复查（双检锁）
│   │   │   ├── 延迟 import MemoryWorker
│   │   │   ├── MemoryWorker(manager=, llm=) + worker.start()
│   │   │   └── 缓存 → 返回
│   │   │
│   │   └── 引用的模块
│   │       └── worker.py · MemoryWorker(manager=, llm=)
│   │             └── 内部持有 manager.py · DefaultMemoryManager（调四操作）
│   │
│   ├── shutdown_memory_worker(timeout) —— 关 worker
│   │   │
│   │   │   用途：drain 队列 + 关线程（等剩余任务处理完或超时）。
│   │   │
│   │   ├── 做什么
│   │   │   ├── 加 _worker_lock
│   │   │   ├── _memory_worker 为 None → 直接返回（幂等）
│   │   │   ├── _memory_worker.shutdown(timeout=)
│   │   │   └── _memory_worker = None
│   │   │
│   │   └── 引用的模块
│   │       └── worker.py · MemoryWorker.shutdown(timeout=)
│   │
│   └── get_memory_worker() —— 取 worker
│       │
│       │   用途：供上层检查 worker 是否已启动，或做额外编排。
│       │
│       └── 做什么
│           ├── 加 _worker_lock
│           └── 返回 _memory_worker（可能为 None）
│
│
└── 内部辅助（装配细节）
    │
    ├── _load_memory_provider(config) —— 装配核心
    │   │
    │   │   用途：把契约 + 实现 + config 拼成可用的 provider。
    │   │        这是主链装配的核心步骤。
    │   │
    │   ├── 做什么
    │   │   ├── 延迟 import build_default_provider（避免强依赖 + 破循环 import）
    │   │   ├── journal = _make_journal_callback()（可能为 None）
    │   │   ├── provider = build_default_provider(journal=journal)
    │   │   ├── _wrap_store(provider.store(), provider.retriever())
    │   │   └── 返回 provider
    │   │
    │   └── 引用的模块
    │       ├── strategies/default/strategy.py · build_default_provider(journal=)
    │       │     ├── decay.py · EbbinghausDecayPolicy()
    │       │     ├── forget.py · CompositeForgetPolicy(decay)
    │       │     ├── store.py · MarkdownFileStore(config.storage_path)
    │       │     ├── retriever.py · HybridRetriever(store, decay)
    │       │     ├── manager.py · DefaultMemoryManager(store, decay, forget, journal)
    │       │     └── strategy.py · DefaultMemoryProvider(_store, _retriever, _manager)
    │       ├── _make_journal_callback()（本文件内部）
    │       └── _wrap_store()（本文件内部）
    │
    ├── _make_journal_callback() —— journal 注入（可降级）
    │   │
    │   │   用途：拿 RunJournal 做事件回调。主链对 journal 是弱依赖，
    │   │        拿不到就静默降级（返回 None），不阻断 provider 装配。
    │   │
    │   ├── 做什么
    │   │   ├── try 内延迟 import get_run_journal
    │   │   ├── journal = get_run_journal()
    │   │   │     None → 返回 None
    │   │   │     否则 → 返回 lambda(event, payload): journal.append(...)
    │   │   └── except → 记 debug 日志 + 返回 None（静默降级）
    │   │
    │   └── 引用的模块
    │       └── agents/journal · get_run_journal()（项目内未提供）
    │             └── RunJournal.append(event, payload)
    │
    └── _wrap_store(store, retriever) —— 装饰器（读写闭环关键）
        │
        │   用途：把 store 的写操作同步到 retriever 的增量索引（5B）。
        │        主链「写路径 → 读路径」的闭环点：
        │        写完立刻能被下一轮检索到。
        │
        ├── 做什么
        │   ├── 保存原始方法：original_add / original_update /
        │   │                 original_batch_update / original_remove
        │   ├── 定义 wrapped_add / wrapped_update /
        │   │        wrapped_batch_update / wrapped_remove
        │   │     每个都是「先调原方法，再调 retriever 钩子」
        │   └── 替换 store 的四个方法（运行时方法替换，不改类）
        │
        └── 引用的模块
            ├── store.py · add / update / batch_update / remove（原方法 + 被替换）
            └── retriever.py · on_trace_added / on_trace_updated / on_trace_removed
                  add          → on_trace_added
                  update       → on_trace_updated
                  batch_update → 逐条 on_trace_updated
                  remove       → on_trace_removed
```

---

## 1. 「三个圈层」

```
┌─────────────────────────────────────────────────────────────┐
│ 对外接口层（Provider lifecycle 4 函数）                        │
│                                                             │
│   get_memory_provider()      ← 主入口，懒加载                 │
│   reset_memory_provider()    ← 测试用                        │
│   shutdown_memory_provider() ← 主出口                        │
│   set_memory_provider(p)     ← 测试用                        │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           │ 首次 get 时触发装配
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ 内部装配层（_load + journal + wrap）                          │
│                                                             │
│   _load_memory_provider(config)                             │
│       ├── _make_journal_callback()   ← 可降级 None           │
│       ├── build_default_provider()   ← 真正装配              │
│       └── _wrap_store()              ← 读写闭环              │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           │ 另一条独立生命周期
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ Worker lifecycle（3 函数，L5）                                │
│                                                             │
│   start_memory_worker(manager, llm)  ← 启 daemon 线程        │
│   shutdown_memory_worker(timeout)    ← drain + 关           │
│   get_memory_worker()                ← 取实例                │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 三个关键点

1. **双检锁**：`get_memory_provider` 和 `start_memory_worker` 都用「无锁快路径 + 有锁复查」，保证线程安全且不重复构建。

2. **两套独立锁**：`_provider_lock` 和 `_worker_lock` 分开，因为 provider 不依赖 llm、worker 依赖 llm，生命周期不同。

3. **`_wrap_store` 是闭环点**：装配完 provider 后立刻装饰 store，把写路径（manager → store）和读路径（retriever）接起来。没有它，本轮沉淀的记忆下一轮检索不到。