---

name: DefaultMemoryManager — 四操作编排（Encode / Associate / Consolidate / Reconsolidate）。

description:

【整体职责】
实现 MemoryManager Protocol，提供记忆的四操作编排。
是 memory 模块唯一的 manager 实现，被 worker（写路径）与 Provider（工具调用）调用。

四操作：Encode / Associate / Consolidate / Reconsolidate。
（Retrieve 不在本模块 —— 检索统一走 Retriever。）

【核心原则】
- 工具里无 LLM：merged_content / new_content 由外部传入。
- frozen 语义：所有操作返回新 MemoryTrace 实例（replace / with_operation）。
- 依赖注入：store + decay_policy + forget_policy + journal。

【traceability（三路）】
- A：每操作 append OperationLog
     （encode / associate / consolidate / reconsolidate / forget）。
- B：每操作 emit journal 事件（memory.*），journal callback 注入。
- C：actor 字段从 ContextVar `_turn_id_var` 取（Layer 4 Middleware 注入 turn_id）。

【关键决策（已拍板）】
- A1：encode 初始 strength = base_strength。
- B3：reconsolidate 保留原 strength + last_accessed=now（视为一次访问）。
- C1：forgotten trace 由 Retriever 过滤（本模块只标记 metadata.forgotten）。
- D3：associate 超 max 时 LRU 淘汰 strength 最低的关联。
- E1：consolidate 数量上限 10。
- F2：encode id = SHA256(content + type.value) 前 16 位（同内容同 type 去重）。
- 点4：encode get-then-add 幂等（同 id 返回已存在 trace）。

【职责边界】
- 本模块只做四操作编排，不做实际存储（那是 store 的事）。
- 不生成内容：merged_content / new_content 由外部 LLM 生成后传入。
- 不判定遗忘：should_forget 由 forget_policy 提供，本模块只在 consolidate 时标 forgotten。

【INVARIANT】
- 四操作无 LLM（consolidate / reconsolidate 的 merged_content / new_content 外部传入）。
- frozen 语义：所有操作返回新实例，不原地修改。
- operation_log 上限 20 条 FIFO（由 schema.MemoryTrace.with_operation 保证）。
- runtime 可切：decay 参数每次从 get_memory_config() 取最新。

---

## 0. 结构树

```
manager.py —— DefaultMemoryManager，四操作编排
│
│   整体职责：实现 MemoryManager Protocol，提供记忆的四操作编排。
│             是 memory 模块唯一的 manager 实现，被 worker（写路径）与 Provider 调用。
│
│   四操作：Encode / Associate / Consolidate / Reconsolidate
│           （Retrieve 不在本模块 —— 检索统一走 Retriever）
│
│   核心原则：工具里无 LLM（merged_content / new_content 外部传入）
│             frozen 语义（所有操作返回新实例）
│
│
├── 模块级
│   │
│   ├── _turn_id_var : ContextVar[str | None]
│   │     当前对话轮次标识（Layer 4 注入 / worker 注入）。
│   │
│   └── set_turn_id(turn_id) → None
│         设置当前 turn_id（关联记忆操作到对话轮次）。
│         Layer 4 MemoryMiddleware before_model 注入 / after_model 清除；
│         worker 处理任务时注入 "worker:{thread_id}:{turn_count}"。
│
│
├── 导入的模块
│   │
│   ├── config.py · get_memory_config()
│   │     每次取最新 decay 参数（runtime 可切）。
│   │
│   ├── exceptions.py · MemoryNotFoundError
│   │     associate / reconsolidate / consolidate 找不到 trace 时抛。
│   │
│   ├── memory_store.py · MemoryStore（Protocol）
│   │     依赖注入的持久化后端。
│   │
│   ├── schema.py · Association / MemoryTrace / MemoryType / OperationLog
│   │     构造 / 派生 trace，记操作日志。
│   │
│   ├── _constants.py · ASSOCIATE_DEFAULTS / CONSOLIDATE_PARAMS / DECAY_PARAMS
│   │     关联上限、合并数量范围、缺省衰减参数。
│   │
│   ├── decay.py · EbbinghausDecayPolicy
│   │     构造时注入（缺省内部构造），encode 时取参数。
│   │
│   └── forget.py · CompositeForgetPolicy
│         构造时注入（缺省内部构造）。
│
│
└── DefaultMemoryManager（MemoryManager 的默认实现）
    │
    ├── 字段
    │   ├── _store : MemoryStore                  ← 持久化后端
    │   ├── _decay_policy : EbbinghausDecayPolicy ← 衰减策略
    │   ├── _forget_policy : CompositeForgetPolicy ← 遗忘策略
    │   └── _journal : Callable | None            ← 事件回调（traceability B）
    │
    ├── __init__(store, *, decay_policy=None, forget_policy=None, journal=None)
    │   │   注入 store + 策略 + journal。
    │   │
    │   └── 引用的模块
    │       ├── decay.py · EbbinghausDecayPolicy()（缺省）
    │       └── forget.py · CompositeForgetPolicy(self._decay_policy)（缺省）
    │
    │
    ├── 内部辅助
    │   │
    │   ├── _compute_trace_id(content, type) → str
    │   │   │   F2：id = SHA256(content + type.value) 前 16 位。
    │   │   │   同内容同 type → 同 id（去重）。
    │   │   │
    │   │   └── 引用的模块
    │   │       └── schema.py · MemoryType（类型转换）
    │   │
    │   ├── _add_association_with_lru(trace, new_assoc, max_assocs) → MemoryTrace
    │   │   │   D3：加关联，超 max 时 LRU 淘汰 strength 最低的。
    │   │   │
    │   │   └── 引用的模块
    │   │       ├── schema.py · MemoryTrace / Association
    │   │       └── replace（frozen 语义）
    │   │
    │   ├── _get_actor() → str | None
    │   │   │   C：取当前 turn_id（Layer 4 注入 ContextVar）。
    │   │   │
    │   │   └── 引用的模块
    │   │       └── _turn_id_var（本文件）
    │   │
    │   ├── _emit_journal(event, payload) → None
    │   │   │   B：emit journal 事件（Layer 4 注入 RunJournal，None 时静默）。
    │   │   │
    │   │   └── 引用的模块
    │   │       └── _journal（构造时注入）
    │   │
    │   ├── _get_decay_params(type) → dict
    │   │   │   取衰减参数（runtime config 优先，缺省回退 _constants）。
    │   │   │
    │   │   └── 引用的模块
    │   │       ├── config.py · get_memory_config()
    │   │       └── _constants.py · DECAY_PARAMS
    │   │
    │   ├── _get_associate_defaults() → dict
    │   │   │   取关联默认参数。
    │   │   │
    │   │   └── 引用的模块
    │   │       └── _constants.py · ASSOCIATE_DEFAULTS
    │   │
    │   └── _get_consolidate_params() → dict
    │       │   取巩固参数。
    │       │
    │       └── 引用的模块
    │           └── _constants.py · CONSOLIDATE_PARAMS
    │
    │
    └── 四操作
        │
        ├── encode(content, type, *, importance=0.5, source=None, metadata=None) → MemoryTrace
        │   │   Encode（编码）：创建新记忆。
        │   │   F2：id = content hash（同内容同 type 去重）。
        │   │   点4：get-then-add 幂等（同 id 返回旧 trace）。
        │   │   A1：strength = base_strength。
        │   │
        │   ├── 步骤
        │   │   ├── 1. F2：算 trace_id
        │   │   ├── 2. 点4：store.get(trace_id) → 已存在则 emit duplicate + 返回旧 trace
        │   │   ├── 3. A1：从 _get_decay_params 取 base_strength
        │   │   ├── 4. actor = _get_actor()
        │   │   ├── 5. 构造 MemoryTrace（operation_log 初含 encode 日志）
        │   │   ├── 6. store.add(trace)
        │   │   ├── 7. emit "memory.encode"
        │   │   └── 8. 返回 trace
        │   │
        │   └── 引用的模块
        │       ├── store.py · get() / add()
        │       ├── schema.py · MemoryTrace / OperationLog
        │       ├── config.py（间接，通过 _get_decay_params）
        │       └── 本文件内部辅助
        │
        ├── associate(trace_id_a, trace_id_b, *, strength=None, type=None) → None
        │   │   Associate（关联）：建立两条记忆的双向关联。
        │   │   D3：超 max 时 LRU 淘汰 strength 最低的关联。
        │   │
        │   ├── 步骤
        │   │   ├── 1. 取默认参数（strength / type / max_assocs）
        │   │   ├── 2. store.get 两条 trace（任一不存在抛 MemoryNotFoundError）
        │   │   ├── 3. 构造双向 Association
        │   │   ├── 4. D3：_add_association_with_lru 更新两条
        │   │   ├── 5. traceability A：两条各 append associate 日志
        │   │   ├── 6. store.update 两条
        │   │   └── 7. emit "memory.associate"
        │   │
        │   └── 引用的模块
        │       ├── store.py · get() / update()
        │       ├── schema.py · Association / OperationLog
        │       ├── exceptions.py · MemoryNotFoundError
        │       ├── _constants.py · ASSOCIATE_DEFAULTS（间接）
        │       └── 本文件内部辅助
        │
        ├── consolidate(trace_ids, merged_content) → MemoryTrace
        │   │   Consolidate（巩固）：多条零散记忆合并为一条 stable 知识。
        │   │   E1：数量校验 [min, max]。
        │   │   C1：旧 trace 标记 forgotten（不删除）。
        │   │   F2：batch_update 一次提交（避免 O(N²)）。
        │   │
        │   ├── 步骤
        │   │   ├── 1. E1：数量校验
        │   │   ├── 2. store.get 全部旧 trace（任一不存在抛 MemoryNotFoundError）
        │   │   ├── 3. 算合并后 importance（max + boost）
        │   │   ├── 4. C1 幂等：算 new_trace_id，已存在则 emit duplicate + 返回
        │   │   ├── 5. 合并关联（去重 union，排除内部关联）
        │   │   ├── 6. 构造新 semantic trace
        │   │   ├── 7. store.add(new_trace)
        │   │   ├── 8. 标记旧 trace forgotten（append "forget" 日志）
        │   │   ├── 9. F2：store.batch_update 一次提交
        │   │   ├── 10. emit "memory.consolidate"
        │   │   └── 11. 返回 new_trace
        │   │
        │   └── 引用的模块
        │       ├── store.py · get() / add() / batch_update()
        │       ├── schema.py · MemoryTrace / MemoryType / Association / OperationLog
        │       ├── exceptions.py · MemoryNotFoundError
        │       ├── _constants.py · CONSOLIDATE_PARAMS（间接）
        │       └── 本文件内部辅助
        │
        └── reconsolidate(trace_id, new_content) → MemoryTrace
            │   Reconsolidate（重编码）：更新已有记忆内容。
            │   B3：保留原 strength + last_accessed=now（视为一次访问）。
            │   frozen 语义：id 不变，content 替换，日志记 content diff。
            │
            ├── 步骤
            │   ├── 1. store.get(trace_id) → 不存在抛 MemoryNotFoundError
            │   ├── 2. B3：replace 替换 content + last_accessed=now
            │   ├── 3. traceability A：with_operation 记 reconsolidate 日志
            │   ├── 4. store.update(updated)
            │   ├── 5. emit "memory.reconsolidate"
            │   └── 6. 返回 updated
            │
            └── 引用的模块
                ├── store.py · get() / update()
                ├── schema.py · MemoryTrace / OperationLog
                ├── exceptions.py · MemoryNotFoundError
                └── 本文件内部辅助
```

---

## 1. 四操作对 store 的调用

```
encode        → store.get（幂等检查）+ store.add
associate     → store.get × 2 + store.update × 2
consolidate   → store.get × N + store.add + store.batch_update
reconsolidate → store.get + store.update
```

---

## 2. traceability 三路

```
A（OperationLog）
    每次操作 append 一条到 trace.operation_log（上限 20 FIFO）
    ├── encode       → operation="encode"
    ├── associate    → operation="associate"（两条各一条）
    ├── consolidate  → operation="consolidate"（新 trace）
    │                   + operation="forget"（旧 trace 各一条）
    └── reconsolidate → operation="reconsolidate"

B（journal 事件）
    每次操作 emit 一条 memory.* 事件
    ├── memory.encode / memory.encode.duplicate
    ├── memory.associate
    ├── memory.consolidate / memory.consolidate.duplicate
    └── memory.reconsolidate

C（actor）
    从 ContextVar `_turn_id_var` 取，写入 OperationLog.actor + journal payload.actor
    ├── Layer 4 MemoryMiddleware：before_model 注入，after_model 清除
    └── worker._process：注入 "worker:{thread_id}:{turn_count}"
```

---

## 3. 被谁引用

```
DefaultMemoryManager
│
├── strategies/default/strategy.py · build_default_provider
│     构造实例，作为 provider 的 manager。
│
├── worker.py · _extract_and_encode / _maybe_consolidate
│     encode / consolidate（写路径）。
│
└── 潜在：Provider 暴露给上层的工具调用（encode / associate / reconsolidate）
```

---

## 4. 总览

```
DefaultMemoryManager  = MemoryManager 的默认实现，四操作编排
set_turn_id           = 模块级函数，注入/清除 turn_id（traceability C）
_compute_trace_id     = F2：content hash 前 16 位
_add_association_with_lru = D3：关联超限 LRU 淘汰最弱
_get_actor / _emit_journal = traceability C / B
encode                = A1 + F2 + 点4（幂等）
associate             = D3（双向 + LRU）
consolidate           = E1 + C1 + F2（数量校验 + 标记 forgotten + batch_update）
reconsolidate         = B3（保留 strength + 记 diff）
```