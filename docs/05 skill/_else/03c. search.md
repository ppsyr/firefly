---

name: unified_search — 跨 source 聚合搜索。

description: 

【整体职责】
hub 的统一搜索入口：把多个 source（builtin + hub sources）的搜索结果聚合成一份。
- 聚合：遍历所有 source，收集 SkillMeta。
- 去重：按 name 去重（先到先得）。
- 排序：is_installed 优先，其次按 name 字典序。
- 截断：取前 limit 条。
- 降级：sources 为空 / builtin 不可用时，只搜 builtin 或返空。

设计（design_docs/46 §2.6）：
- 跨 source 聚合搜索（builtin + hub sources 并行）。
- 去重（按 name）+ 排序（is_installed 优先）+ limit 截断。
- hub 不可用时降级为只搜 builtin。

【内容摘要】
- unified_search(query, sources, limit) -> list[SkillMeta]
    聚合搜索主函数，返回 SkillMeta 列表。
- unified_search_as_dicts(query, sources, limit) -> list[dict]
    dict 版本（供 JSON 返回）。

【职责边界】
- 只负责：跨 source 聚合、去重、排序、截断、降级。
- 不负责：具体 source 实现（在 sources/ 下）、安装（installer.py）、
  provenance（HubLockFile）、安全扫描（SkillsGuard）、审计（AuditLog）。
- 不做并行：当前是顺序遍历 sources（不是并发）。
- 不做持久化：只返结果，不落库。

---

## 0. 结构树

### 0.1. 静态结构树

```
skill/hub/search.py — unified_search（跨 source 聚合搜索）
│
├─ unified_search(query, sources=None, limit=10)          ← 【对外主入口】聚合
│   -> list[SkillMeta]
│   ├─ sources 为空 → 降级为 [BuiltinSource()]
│   │     └─ builtin 不可用 → 返 []
│   ├─ for source in sources:
│   │     ├─ source.search(query, limit=limit)
│   │     ├─ 按 name 去重（空 name 跳过）
│   │     └─ source 异常 → logger.warning + 跳过
│   ├─ 排序 key：(not is_installed, name)                  ← is_installed 优先
│   └─ limit 截断
│
└─ unified_search_as_dicts(query, sources=None, limit=10)  ← 【dict 版本】供 JSON 返回
    -> list[dict]
    └─ 调 unified_search → 投影 8 字段：
          name / description / category / source /
          identifier / is_installed / install_path / preview_url
```

---

### 0.2. 运行时调用树

```
① 装配期（bootstrap）
    sources = [
        BuiltinSource(),
        GitHubSource(),
        ClaudeMarketplaceSource(),
        WellKnownSource(endpoints),
    ]
        └─ 传给 CLI / TUI / agent_tools（作为 sources 参数）

② 搜索（CLI / TUI / Agent 工具触发）
    unified_search(query, sources, limit)          ← 本模块主入口
        ├─ sources 为空 → 降级 builtin
        ├─ for source in sources:
        │     └─ source.search(query, limit)
        │           ├─ BuiltinSource.search     → SkillManager
        │           ├─ GitHubSource.search      → MVP 返空
        │           ├─ ClaudeMarketplaceSource.search → registry
        │           └─ WellKnownSource.search   → 多 endpoint
        ├─ 去重（按 name）
        ├─ 排序（is_installed 优先）
        └─ limit 截断

③ dict 版（JSON 返回给 CLI / TUI / 工具）
    unified_search_as_dicts(query, sources, limit)
        └─ unified_search → 投影为 list[dict]

④ 下游（installer.py / CLI）
    ├─ 展示结果（含 is_installed 标记）
    └─ 用户 / LLM 选 identifier → installer.install(meta)
```

---

## 1. 补充

- **它是 hub 的统一搜索门面**：`sources` 由 bootstrap 注入（4 个 source 的列表）；`unified_search` 不关心具体 source 类型。
- **降级有两层**：
  - `sources` 为空 → 自动构造 `[BuiltinSource()]`（至少能搜内置）。
  - `BuiltinSource` 构造失败 → 返 `[]`。
- **单 source 容错**：某个 source 抛异常 → `logger.warning` + 跳过，**不影响其他 source**。
- **去重规则**：按 `name`，先到先得；空 `name` 跳过。所以 sources 的**顺序会影响去重结果**（先遍历到的优先）。
- **排序规则**：`(not is_installed, name)`——已安装的排前面，其次按名字升序。**排序在去重之后、截断之前**。
- **顺序非并行**：当前实现是 `for source in sources`，不是 `asyncio.gather`；所以慢 source 会拖慢整体。设计文档说"并行"，实现是"顺序"，这是 MVP 简化。
- **两个函数是同一逻辑的两种视图**：`unified_search` 返 `SkillMeta`（内部用）；`unified_search_as_dicts` 返 `dict`（给 JSON 序列化 / 工具返回值用）。
- **它在整个 hub 流程中的位置**：`unified_search`（找技能）→ 用户/LLM 选 → `installer.install`（装技能）→ `SkillsGuard.scan`（安全）→ `HubLockFile.add` + `AuditLog.append`（记录）。