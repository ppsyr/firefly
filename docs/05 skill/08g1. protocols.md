---

name: 门控 Protocol（2b/L3 预留）。

description: 

【整体职责】
定义 evolution 闭环"门控"环节的 Protocol（接口契约），为 2b / L3 预留。
2a 已实现 ScoreDeltaGate + GitRatchet（gates/score_delta_gate.py + gates/git_ratchet.py）；
本模块的 Protocol 用于未来替换 / 扩展，实现时零侵入。

5 个 Protocol：
- ChampionGateProtocol  （2b）：score delta + hard_failures 双门（区分关键 vs 非关键）。
- HITLGateProtocol      （2b）：人审装饰器，accept 转 pending_human staging。
- CompositeGateProtocol （2b）：cascading 链式，早期 reject short-circuit。
- ValidationGateProtocol（L3）：held-out 重放 + longitudinal pairs，
                                需 L3 ValidationGateEvalAdapter。
- MultiJudgeGateProtocol（L3）：多 LLM judge + majority vote，
                                需 L3 LLMJudgeEvalAdapter。

所有门实现 PromotionGate Protocol（evolution/protocols.py）。

【内容摘要】
- ChampionGateProtocol  : decide(candidate, baseline, eval_result) -> GateDecision。
- HITLGateProtocol      : 同上（人审装饰）。
- CompositeGateProtocol : 同上（链式组合）。
- ValidationGateProtocol: 同上（held-out 重放）。
- MultiJudgeGateProtocol: 同上（多 judge 投票）。

【职责边界】
- 只定义接口（Protocol），不含实现。
- 不负责：门控决策逻辑（在实现类里）、评估（eval_bridge）、变异（mutator）、
  持久化（store）。
- 2a 已实现的门（ScoreDeltaGate / GitRatchet）不在本模块；
  本模块仅列 2b / L3 预留的 Protocol。
- 不 runtime import 实现类：实现由 bootstrap 注入。

---

## 0. 结构树

### 0.1. 静态结构树

```
skill/evolution/gates/protocols.py — 门控 Protocol（2b/L3 预留）
│
├─ class ChampionGateProtocol(Protocol, runtime_checkable)   ← 【2b】score delta + hard_failures 双门
│   └─ decide(candidate, baseline, eval_result) -> GateDecision
│
├─ class HITLGateProtocol(Protocol, runtime_checkable)       ← 【2b】人审装饰器
│   └─ decide(candidate, baseline, eval_result) -> GateDecision
│       └─ accept 转 pending_human，staging 待 /skill approve|reject
│
├─ class CompositeGateProtocol(Protocol, runtime_checkable)  ← 【2b】链式组合
│   └─ decide(candidate, baseline, eval_result) -> GateDecision
│       └─ cascading，早期 reject short-circuit
│
├─ class ValidationGateProtocol(Protocol, runtime_checkable) ← 【L3】held-out 重放
│   └─ decide(candidate, baseline, eval_result) -> GateDecision
│       └─ 需 L3 ValidationGateEvalAdapter
│
└─ class MultiJudgeGateProtocol(Protocol, runtime_checkable) ← 【L3】多 judge 投票
    └─ decide(candidate, baseline, eval_result) -> GateDecision
        └─ 需 L3 LLMJudgeEvalAdapter
```

---

### 0.2. 运行时调用树

```
① 装配期（bootstrap）
    ├─ 2a：ScoreDeltaGate / GitRatchet（已实现，不在本模块）
    └─ 2b / L3：实现本模块的 Protocol → 注册到 EvolutionManager

② 进化闭环第五步 —— 门控（EvolutionManager._run_evolution 内）
    EvolutionManager._run_evolution(ctx)
        ├─ …（focus → mutate → eval）
        └─ gate.decide(candidate, baseline, eval_result)    ← 本模块 Protocol 的调用点
              ├─ accept / accept_new_best → store.create_version
              └─ reject / pending_human   → 仅记录，不写 version DAG

③ 各 Protocol 的语义差异（同一调用点，不同实现）
    ChampionGate   ：score delta + hard_failures 双门
    HITLGate       ：accept 转 pending_human → /skill approve|reject
    CompositeGate  ：链式多门，早期 reject short-circuit
    ValidationGate ：held-out 重放 + longitudinal pairs（L3）
    MultiJudgeGate ：多 LLM judge + majority vote（L3）
```

---

## 1. 补充

- **本模块只定义接口，不含实现**：2a 实际用的是 `ScoreDeltaGate` + `GitRatchet`（在 `gates/score_delta_gate.py` 和 `gates/git_ratchet.py`），本模块列的是 2b / L3 预留。
- **统一的调用签名**：所有门都实现 `PromotionGate Protocol`（`evolution/protocols.py`）的 `decide(candidate, baseline, eval_result) -> GateDecision`，所以 `EvolutionManager` 无需感知具体实现。
- **`CompositeGate` 与 `HITLGate` 的组合关系**：CompositeGate 可把多个门链起来（如 ScoreDeltaGate + HITLGate + ValidationGate），早期 reject 直接短路。
- **L3 门的额外依赖**：`ValidationGate` 需 `ValidationGateEvalAdapter`；`MultiJudgeGate` 需 `LLMJudgeEvalAdapter`——它们把 L3 的评估能力接进门控。
- **调用点在 `_run_evolution` 第五步**：`gate.decide(...)` 的返回决定是否 `create_version`；这是 accept / reject 的分界。